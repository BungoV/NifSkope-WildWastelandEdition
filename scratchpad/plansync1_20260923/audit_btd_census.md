# PLANSYNC1 audit -- .lodl (BTD) + CENSUS claims + FO4CS side (read-only)

Auditor: read-only sub-agent, 2026-09-23. Plan = docs/FO4CS_IMPROVED_LOD_PLAN.md (1,605 lines).
BTD = docs/LODGEN_BTD_FORMAT.md (1,424 lines). CENSUS = docs/LODGEN_CENSUS.md (510 lines).

## A. `.lodl` truth (from source)

* Magic: `LODL_MAGIC = 0x54444F4CU` ('LODT' LE) -- src/lodtfile.h:29 `constexpr quint32 LODL_MAGIC = 0x54444F4CU;`
* Writer/reader range: src/lodtfile.cpp:57-58 `constexpr quint32 LODL_VERSION = 3;` / `LODL_VERSION_MIN = 1;` -- reader refuses outside 1..3 (lodtfile.cpp:2796 `unsupported version %1 (this reader knows %2..%3)`); texture file magic LDTX refused BY NAME (lodtfile.cpp:2778).
* Writer default: src/lodtfile.h:159 `int headerVersion = 2;` ; water module `LodtWaterOptions::enabled = false` (lodtfile.h:91, CLI `--water-bodies` nifcli.cpp:7922; GUI QSettings `LodGeneration/waterBodies` default false, lodgenmanager.cpp:1620). A default bake writes **v2**.
* Version raise: lodtfile.cpp:1707 `if ( wantWater && version < 3 ) version = 3;` -- ONLY the water module raises it.
* Env override: `WW_LODL_VERSION` (lodtfile.cpp:1717-1718), applied AFTER the water raise, range-checked 1..3 (1719); old spelling `WW_LODT_VERSION` is REFUSED by name (1713).
* Header sizes: v1 0x98 (152), v2 0xA0 (160), v3 0xF8 (248) -- lodtfile.cpp:59-64, table `lodtHeaderBytes()` 75-83.
* v2 added: worldspace default water height + WATR form after the ten section offsets (lodtfile.h:147-150; BTD 191-198).
* v3 added: water bodies -- body table + body-ID plane, flow plane, shore-distance plane, stroke store (section bits 4..7), fields appended from 0xA0, sections after block data (BTD 15-18, 183-189). Later, still v3: dye plane, bit 8, offset in reserved word 0xF4, generator always writes 0 (lodtfile.h:46-51; BTD 168).
* Row order: ROW 0 IS SOUTH in every grid (blocks, overview, AO, v3 planes) -- BTD 220-226 `**ROW 0 IS SOUTH**, in every grid this file carries`; lodtfile.cpp:187 `Row 0 is SOUTH (the grid's own order`. Opposite of the north-up HeightMap DDS and of `.lodt`.

## A. Audit table

| plan line | claim | current truth | source anchor | verdict |
|---|---|---|---|---|
| 88 | `.lodl` = heights, per-texel LTEX opacities, per-cell water, AO, and (v3) water bodies; defined by BTD Header, BTD Blocks; on disk, FO4CS already reads it | Matches. v3 also carries flow/shore/stroke (+ dye bit 8) -- "water bodies" is BTD's umbrella name for all of them | BTD:15-18 `adding WATER BODIES - a body table, a per-texel body-ID plane, flow, shore distance and a stroke store`; sections `## Header` (119), `## Blocks` (387) exist | OK on content; "FO4CS already reads it" is STALE -- FO4CS reads only a v1 file at `Data\Terrain\<EDID>.lodt` (old name, old folder; section B), so it reads no file a bake writes today |
| 145-150 (§1.5) | FO4CS `FarFieldLodtFormat.h` + `FarFieldLodtSource.h` in wt-fixfirst; `kVersion` is `1u`, refuses anything else; writer defaults to 2; fallback `WW_LODL_VERSION=1` | Our side: writer default 2 and WW_LODL_VERSION=1 fallback both TRUE (lodtfile.h:159; lodtfile.cpp:1717). FO4CS side: see section B | BTD:27-36 `A CONSUMER EXISTS, AND IT KNOWS VERSION 1 ONLY` | see B |
| 151-155 (§1.5) | `FarFieldLodBtoChannels.h` decodes `.bto` channels | FO4CS side, see B | -- | see B |
| 183-186 (R0 READS) | reads "the `.lodl` header (BTD Header)" | `## Header` section exists (BTD:119); v1/v2/v3 header table present | BTD:119 `## Header` | OK |
| 191 (R0 DOES hard list) | "version (1 and 2 refused BY NAME)" -- applies to .lodo/.lodi | Not a .lodl claim; for .lodl, 1..3 are all LEGAL and must be accepted. Plan never states the .lodl hard/soft split in R0 DOES | lodtfile.cpp:2796 | OK for .lodl (no .lodl claim); R0-IMPACT note: R0 DOES should say the .lodl accepts 1..3 and fails closed on unknown version via header-size table |
| 203-204 (R0 DOES) | `.lodt` reader 22-rule refusal list (VT 3.4) | out of this audit's scope (VT) | -- | not audited |
| 241-242 (R0 gate 5) | `.lodl` parser accepts v2 and v3 (BTD Header); today's FO4CS parser pins 1 and refuses both | Our side true. FO4CS side: see B | BTD:19 `the reader accepts 1, 2 and 3` | OK (our side) |
| 404-406 (R2 READS) | `.lodl` for inner band LTEX opacities, heights, water planes (BTD Blocks, BTD Tables) | `## Blocks` (387) and `## Tables` (292) exist. Row 0 SOUTH not mentioned in R2 -- a north-up reader must mirror Y (BTD 223) | BTD:220 `**ROW 0 IS SOUTH**` | OK; R0/R2-IMPACT: plan never states row order for `.lodl` |
| 949 (§3 seam 2) | inner band from `.lodl` FINEST level equals LAND heights exactly (BTD Height encoding: quantum 8 exactly lossless vs VHGT) | Quantum-8 lossless claim TRUE (BTD:262). Nuance: the 33rd row/column is stored once as the MAX over the cells carrying it (BTD "Shared edges"); identical in well-formed data, differs on 439 rim filler cells; a landless cell inherits edge + default land height | BTD:262 `so this is **exactly lossless**`; BTD:528-536 `holds the **maximum over every cell that carries the sample**` | OK (nuance: cite BTD "Shared edges" too) |
| 1065 (§5 row 16) | shipped FO4CS `.lodl` parser pins `kVersion = 1u`, refuses 2 and 3; way back `WW_LODL_VERSION=1` | Our side true. FO4CS side: see B | lodtfile.cpp:1717 | see B |
| 1249 (§8 version row) | "writer defaults **2**, reader accepts **1, 2, 3**, magic `LODT`" | TRUE: headerVersion = 2; LODL_VERSION_MIN 1 / LODL_VERSION 3; magic 0x54444F4C. Missing from the row: v3 is written only with `--water-bodies`; env override WW_LODL_VERSION (1..3; WW_LODT_VERSION refused); `LDTX` texture magic refused by name | lodtfile.h:159 `int headerVersion = 2;`; lodtfile.cpp:57 `constexpr quint32 LODL_VERSION = 3;` | OK |

Minor BTD-page nit (not a plan claim): BTD:200-202 section-present-flags paragraph lists bits 0..7 only; bit 8 (dye) is documented only in the 0xF4 header row (BTD:168) and lodtfile.h:51.

## B. FO4CS side today (read-only; nothing written, no git mutation)

* Trees: main checkout `fallout4-community-shaders` is on `projectile-recovery-52a` @ 1cd4386 (WIP, 2026-08-30) and has NO `src/FarField/FarFieldLodtFormat.h`. The deployed line is `wt-fixfirst` = branch `fixfirst-20260817` @ b0719da (carries wave 86 head 90e9b2a). 43 worktrees listed by `git worktree list`.
* `.lodl` parser: `wt-fixfirst/src/FarField/FarFieldLodtFormat.h:65-66` `kMagic = 0x54444F4Cu;` `kVersion = 1u;  // lodtfile.cpp:39`; `:70` `kHeaderBytes = 0x98u`; `:378` `if (h.version != kVersion) { return refuse(Reject::kUnsupportedVersion);` -- accepts v1 ONLY, refuses 2 and 3 (and does not name the LDTX texture magic; it is a plain kBadMagic). Plan §1.5 / §5 row 16 / §8 claim = **still TRUE**. Same kVersion = 1u in every worktree that carries the file (19 trees incl. wt-publish @ 9fbf3fd "bungo" line, wt-lod0).
* `FarFieldLodBtoChannels.h`, `FeatureModule.h`, `tests/lodt_format_tests.cpp` all exist in wt-fixfirst (§1.5 OK).
* **Path composition (new finding, sharper than plan §5 row 18):** `wt-fixfirst/src/FarField/FarFieldLodtSource.h:533` `kLodtDirPrefix = "Data\\Terrain\\";` and `:539` `path += ".lodt";` -- the shipped parser opens `Data\Terrain\<EDID>.lodt`: the PRE-2026-09-09 EXTENSION (`.lodt` now names the LDTX texture sheets) AND the pre-LAYOUT1 folder. It never looks at `Data\FO4CSLOD\<WS>\<WS>.lodl`. Caller: `FarFieldHeightmapRuntime.cpp:1460` (`TryBuildFromLodt`, the far-field SHADOW heightmap source, walks parent worldspaces by EDID). It is NOT inside an Improved LOD module. Nothing uses `FO4CSLOD` anywhere in FO4CS src.
* Improved LOD work in progress: lane IMPROVEDLOD-R0 is LIVE today. `Codex/brief_improvedlod_r0.md` (07:54) + `Codex/lane_improvedlod_r0/progress.md` (08:15, 9 lines, design stage, no code). Worktree `wt-lod0`, branch `improvedlod-r0-20260923` @ b0719da, clean (no edits yet). grep for ImprovedLOD / LODO_MAGIC / 0x4F444F4C / FO4CSLOD in wt-lod0/src = 0 hits. Its progress already records: LODO 4 (1,2,3 refused), LODI 3..9 (8 retired/read-only), LODL 1..3 (lodtfile.cpp:57-58), LDTX 2, LODM 1; and that the gate-1 pair on disk (native1b_20260911) is .lodo v3 / .lodi v3 = refused by today's contract. The brief tells it `.lodl (accept v2 and v3; v1 named)` and path `Data\FO4CSLOD\<ws>\`. **R0-IMPACT:** our reader ACCEPTS v1 (LODL_VERSION_MIN = 1) and `WW_LODL_VERSION=1` is the documented zero-effort way back (BTD:34, plan 150/1065); if R0 refuses v1 "by name" that way back stops working for the new module. Plan gate 5 only says "accepts 2 and 3" -- v1's status for the new loader is not ruled on anywhere I found.
* So today NO FO4CS code accepts any .lodo/.lodi/.lodt(LDTX)/.lodm; the only reader is the v1-only `.lodl` one above.

## C. CENSUS claims (docs/LODGEN_CENSUS.md, 510 lines)

Field count, re-derived by script over the first backticked cell of every table row in CENSUS §2-§5:
§2 `[ImprovedLOD]` 11 | §3 Ring 9 | §4 Cluster 9 | §5.1 Cards 5 | §5.2 Residency 10 | §5.3 Shadow **8** | §5.4 Fade 11 = **63 field names in seven rows**. The three not there at plan time are all in §5.3: `shadowCasters[src]`, `shadowMarchMs`, `shadowMapMs` (60 + 3 = 63). §6.2 cross-check table = **16 rows** (cutOverlap/cutGap share one row).

| plan line | claim | current truth | source anchor | verdict |
|---|---|---|---|---|
| 214-220 | four arm words `arm.objects/terrain/cards/arrays`, each `native`/`stock-fallback`/`off`; refusal vocabulary `no_lodo, no_lodi, no_lodm, no_lodt, no_sheet, no_layer, no_array, no_uv2, refused_hard, no_worldspace` (CENSUS 2) | Same four words, same three values; union of the four refusal columns = exactly those ten words (+ `off`). Per arm: objects `no_lodo,no_lodi,refused_hard,no_worldspace`; terrain `no_lodm,no_lodt,refused_hard`; cards `no_sheet,no_layer`; arrays `no_array,no_uv2`. Default `unwired` | CENSUS §2 table, row `arm.objects` ... `off`, `no_lodo`, `no_lodi`, `refused_hard`, `no_worldspace` | OK |
| 222-225 | whole `[ImprovedLOD]` row = arm x4, worldspace, version, pairing, stale, staleFiles, frames, windowMs (CENSUS 2); staleFiles is a LIST | 11 fields, identical names; staleFiles "list" | CENSUS §2 `staleFiles` \| list | OK. **R0-IMPACT (census page itself stale):** the `version` field still reads "`lodo3/lodi3`", refusals `refused:v1`, `refused:v2`. Source today = .lodo 4 (refuses 1,2,3 by name), .lodi 3..9, default bake .lodi v7. The row has NO slot for the `.lodl` (or `.lodt`) version. Owed to a census-page lane; not ruled |
| 331-333 | `[ImprovedLOD.Ring]` in full: ringEdges ... trianglesTotal (CENSUS 3) + the four-way law | 9 fields, identical; law identical | CENSUS §3 `ringInstances[r] == ringCulledFrustum[r] + ringCulledOccluder[r]` | OK |
| 340-342 | `draws`, `overflow` (CENSUS 4); `drawnPrimitives`, `engineFarHidden` (CENSUS 5.4); `shadowDraws`, `shadowIdentityUnique` (CENSUS 5.3) | all present in the named sections | CENSUS §4, §5.3, §5.4 | OK |
| 342-344 | "This rung adds one census word the page does not yet carry -- a per-SOURCE caster count" | NOW CARRIED: `shadowCasters[src]` (tree/card/mesh/terrain partition) in §5.3, with bake denominator `native-casters:` (§6.1) and two §6.2 checks | CENSUS §5.3 `shadowCasters[src]` \| count \| far-shadow casters this frame **per SOURCE** | STALE -> "R1 fills `shadowCasters[src]` (CENSUS 5.3), checked against the `native-casters:` line (CENSUS 6.1, 6.2)" |
| 355-356 (R1 gate 4, cited CENSUS 5.3) | `shadowIdentityUnique` reads 1; doctoring a duplicate IDENTITY makes it 0 | MEANING CHANGED 2026-09-18 17:4x (director): unique per **GROUP** (.lodi v7, NATIVE 4.9); refuter = doctor two different groups onto one id; on v3-6 files it falls back to per-placement and says so | CENSUS §5.3 `carried a unique **GROUP** (`.lodi` v7, NATIVE 4.9)` | STALE -> "doctoring two different GROUPS onto one group id makes it read 0; on a v3-v6 `.lodi` it falls back to the instance index and says so" |
| 469-470 | `[ImprovedLOD.Residency]`: tilesResident, tilesPresent, tileBytes, residentBytes, evictions (CENSUS 5.2) | all five exist; the row has 10 fields (also libraryBytes, libraryBudgetBytes, instanceBytes, cardBytes, arrayBytes). Plan §4.1 says R2 fills Residency ENTIRE while R4 separately owns `cardBytes` -- internal overlap | CENSUS §5.2 | OK (subset); note §4.1 overlap |
| 627-629 | `[ImprovedLOD.Cluster]` in full: clusterTolerancePx, projectionScale, levelMax, clusterSelected, cutOverlap, cutGap, clusterTriangles, draws, overflow (CENSUS 4) | identical 9 | CENSUS §4 | OK |
| 629-631 | `[ImprovedLOD.Fade]` in full: four thresholds, fadeHysteresisPct, crossFadeInFlight, crossFadePeak, gridEdgeBand (CENSUS 5.4) | §5.4 has 11: those 8 plus suppressedDraws, drawnPrimitives, engineFarHidden (owned by R5/R1 in the plan) -- "in full" is 8 of 11 | CENSUS §5.4 | OK in substance; wording "in full" inexact |
| 631-633 | `[ImprovedLOD.Shadow]` in full: shadowTolerancePx, shadowClusters, shadowTriangles, shadowDraws, shadowIdentityUnique (CENSUS 5.3) | §5.3 now has 8: + `shadowCasters[src]`, `shadowMarchMs`, `shadowMapMs` | CENSUS §5.3 rows `shadowMarchMs` \| ms, `shadowMapMs` \| ms | STALE -> list all 8 |
| 633-635 | "adds two census words the page does not carry: march cost and shadow-map cost" | NOW CARRIED: `shadowMarchMs`, `shadowMapMs` (ms, runtime-timed, default `unmeasured`, refusals `off`/`n/a`; plan's 0.3-0.8 / 1-3 ms labelled ESTIMATES) | CENSUS §5.3 `the terrain shadow **march** alone` | STALE -> "R3 fills `shadowMarchMs` and `shadowMapMs` (CENSUS 5.3)" |
| 787-788 | Cards: cardsDrawn, cardsTotal, cardBases, cardFrames, cardsAggregate (5.1) + `cardBytes` in residency | identical 5 + cardBytes in §5.2 | CENSUS §5.1, §5.2 | OK |
| 917 | `gridEdgeBand`, `suppressedDraws` (CENSUS 5.4) | both in §5.4 | CENSUS §5.4 | OK |
| 963-964 (§4) | "Sixty field names in seven rows" | **63** field names in seven rows | script count above | STALE -> "Sixty-three field names in seven rows" |
| 971-978 (§4.1) | rung table | R1 row lacks `shadowCasters[src]` (plan R1 prose owns it); R3 "Shadow entire" now silently includes it too (double ownership) and includes the two ms words; R3 "Fade entire" overlaps R1's drawnPrimitives/engineFarHidden and R5's suppressedDraws; R2 "Residency entire" overlaps R4's cardBytes | CENSUS §5.3/§5.4/§5.2 | STALE -> R1 row add `shadowCasters[src]`; R3 row: "Shadow: shadowTolerancePx, shadowClusters, shadowTriangles, shadowMarchMs, shadowMapMs" (the rest owned by R1) -- ownership split is a proposal, not ruled |
| 982-988 (§4.2) | six rules = CENSUS 1.2 | CENSUS 1.2 still lists 6 numbered rules, five defaults `uncounted/unread/unset/unchecked/unwired`, `0` only as a measurement. Note §5.3 now also uses a sixth default word `unmeasured` (shadowMarchMs/MapMs) not in §1.2's list | CENSUS §1.2 `are the five defaults used here` | OK (census-internal nit: `unmeasured`) |
| 999 (§4.3) | "CENSUS 6.2 states thirteen" | §6.2 has **16** rows (incl. two `shadowCasters` rows and `libraryBytes`) | CENSUS §6.2 table | WRONG/STALE -> "CENSUS 6.2 states sixteen" |
| 1001-1012 (§4.3 list) | 8 checks quoted | each matches a §6.2 row verbatim in substance | CENSUS §6.2 | OK |
| 1030 | card fields today every one refuses (CENSUS 5.1) | still says so ("Today every card field must refuse"). But `.lodo` v4 now has `cardCount` at 0xD0 (§6.3 item 3 DONE) -- a real denominator for `cardBases`; §6.2's `cardBases` row still says "bases with cardLayer != 0xFFFF" | CENSUS §5.1 `Today every card field must refuse`; §6.3 item 3 | OK |
| 1034-1039 (§4.5) | checker `tests/spells/lodgen_census_check.py`; 59 checks, 0 failures, 31 not-derivable on both Sanctuary pairs (CENSUS 7) | File exists (22,708 B, dated 2026-09-11); CENSUS §7 still states 59/0/31 on the NATIVE1b and LODUI1 pairs. BUT those pairs are `.lodo` v3 and the checker's independent decoder (`tests/spells/lodgen_native_decode.py`, dated 2026-09-19) now refuses v3 by name (`:100` `version 3: ... Re-bake; this reader knows version 4`; `:104` `if h['version'] != 4`), and the checker exits "census REFUSED" on a decoder refusal (`lodgen_census_check.py:396`). The 59/31 figure is historical; not re-runnable on the cited pairs today; no v4 re-measurement found on the page | CENSUS §7 `**59 checks, 0 failures, 31 census words the files cannot carry.**` | STALE (historical number on a pair today's decoder refuses; not run by me) |
| 1066 (§5 row 17) | three census words owed: caster count per SOURCE, march cost ms, shadow-map cost ms | ALL THREE NOW IN CENSUS §5.3 (`shadowCasters[src]`, `shadowMarchMs`, `shadowMapMs`) with §6.2 checks for the caster count | CENSUS §5.3 | STALE -> strike through as DONE (census page), like rows 1-5/19 |

### What the census page gained since 2026-09-11 that the plan's rung tables do not list

Runtime-row fields (belong in §4.1 / rung CENSUS paragraphs): `shadowCasters[src]` (R1), `shadowMarchMs`, `shadowMapMs` (R3); `shadowIdentityUnique` changed meaning to GROUP.
NO runtime field exists for: vertex AO (.lodi v6), vertex sky (v7), scrappable (v9), placement AO (v5), slotInstances, fullTriangles, cardCount, watertight. Those live only on the bake side:
* §6.1 new bake lines: `native-library:` (v4), `native-library-build:`, `native-casters:`, `vt: ... terrainObjectAo` clause (objAo* words), `native-group:` groups + vertex sky (`vertexSkyBytes, vertexSkyPlacements, groups, groupedPlacements, largestGroup, singletonGroups`), identity-join clause, `native-scrappable:` (`scrappablePlacements` ...), layout clause, `bake-record:`, `incremental:`, `native cache:`; horizon words RETIRED (HORIZONOUT 2026-09-19).
* §6.3 items marked DONE: 1 `slotInstances[4]` (.lodi v5, 0xD4..0xE3, reader refuses sum != instanceCount), 2 `fullTriangles` (.lodo v4, v3 refused by name), 3 `cardCount` (.lodo v4 0xD0), 5 `LODO_MESH_WATERTIGHT = 4` (.lodo v4), 8 placement-AO u8 blob (.lodi v5, 0xFF = NOT MEASURED). Items 4, 6, 7, 9 still open/informational.
* §6.2 has NOT been extended to use those new header words (no check uses slotInstances for ringInstances, cardCount for cardBases, fullTriangles for clusterTriangles). Candidate census-page follow-up, not ruled.