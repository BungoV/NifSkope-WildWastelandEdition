# PLANSYNC1 -- audit of the VT-sourced claims in docs/FO4CS_IMPROVED_LOD_PLAN.md

Read-only auditor, 2026-09-23 (clock read 08:28 CEDT). Plan = docs/FO4CS_IMPROVED_LOD_PLAN.md (1,605 lines).
VT = docs/LODGEN_TERRAIN_VT.md (3,032 lines). Source = src/io/lodvfile.{h,cpp} (255 / 842 lines), src/lodgen.{h,cpp}, src/nifcli.cpp.
Marks: OK / WRONG / STALE-NUMBER / SECTION-MOVED. R0-IMPACT = changes what the FO4CS loader's header/version/refusal parsing must do.

## Table

| plan line | plan claim (short) | mark | current truth | source (anchor) |
|---|---|---|---|---|
| 89 | `.lodt` path `Data\FO4CSLOD\<EDID>\<EDID>.VT.<dim>.lodt`; colour, msn, mask, opt. height, opt. emissive; VT 3.1, VT 2.2 | OK | path, sheets and sections all match | VT 3 line 2090 "`Data\FO4CSLOD\<EDID>\<EDID>.VT.<dim>.lodt`, one per level"; VT 2.2 table lines 913-919 |
| 89 | on disk? "no -- VT 7: nothing has ever been written to disk" | WRONG | A whole-Commonwealth v2 pyramid was written today by lane VTBAKE1 into a scratch tree: `scratchpad/vtbake1_20260923/bake/FO4CSLOD/Commonwealth/Commonwealth.VT.{2,4,8,16,32}.lodt` + `.VT.lodm`, all mtime 2026-09-23 08:17; sizes 3,020,120,160 / 755,028,064 / 188,756,064 / 47,186,016 / 11,796,576 B, index 2,505 B. 5 levels, 12,276 tiles all present, 4 sheets (height ON), cover OFF (0 cover tiles). Also: five v1 containers (magic LDTX, version 1, 4 sheets) dated 2026-09-10 sit in `scratchpad/handoff_fo4cs/samples/vt/Terrain/` (746,752 / 11,838,720 / 374,016 / 2,983,168 / 1,492,224 B + a 1,964 B `.lodm`) -- the v2 reader refuses those by name. VT 7 itself still says "None exist" (VT stale). Not in bungo's mod folder. | bake.log last line "end 2026-09-23 08:18:04 rc=0 wall_s=569"; bake.log `vt:` line "levels 5 tiles 12276 present 12276 coverTiles 0 bytes 4022886880 ... sheets 4 emissive none"; VT 7 line 2520 "**None exist.**" |
| 90 | VT `.lodm` path `Data\FO4CSLOD\<EDID>\<EDID>.VT.lodm`; VT 4, LODM 5 | OK | the writer puts the index in the same world folder (VTBAKE1 output proves it). VT 4 still prints the OLD path `Data\Terrain\<EDID>.VT.lodm` and VT 5 `--vt DIR` still says "under `<DIR>/Terrain/`" -- VT is stale there, the plan is right. LODM 5 = "`kind: \"terrainVT\"` -- the pyramid index" | src/lodgen.cpp:12158 `const QString dir = lodgenFo4csWorldDir( outDir, ws );`, :12621 `QString( "%1/%2.VT.lodm" )`; VT line 2348 "**Path:** `Data\Terrain\<EDID>.VT.lodm`" (stale); docs/LODGEN_LODM_FORMAT.md:327 |
| 90 | on disk? "no -- VT 7" | WRONG | same as row above: `Commonwealth.VT.lodm` 2,505 B, 2026-09-23 08:17 (VTBAKE1 scratch tree) | as above |
| 107-112 | hybrid by band; inner band blended at runtime from `.lodl` LTEX opacities; beyond samples the pyramid; formula stated ONCE in VT 2.5; generator gates against an independent implementation | OK | unchanged | VT 2.5 lines 1142-1147 "the runtime's formula is stated HERE, once, and the generator gate below reproduces it" |
| 404-406 | READS header, tile table, payloads (VT 3.1, 3.2, 3.3) through the `.lodm` (VT 4) | OK | section numbers still exist. NOTE the VT page now orders 3.3, then **3.5** (role 7 horizon, retired), then 3.4 | VT headings 2105 / 2169 / 2193 / 2248 / 2292 / 2346 |
| 410-412 | "A tile is **323,680** B without ground cover and **369,920** with it, at content 256 / border 8 / 2 mips (VT 3.3)" | WRONG | Those two figures are tiles WITH the opt-in `--vt-height` sheet. A default bake (no `--vt-height`) is **138,720 B** without cover and **184,960 B** with cover; cover + emissive + height is 416,160. A tile's size is a function of the header's `sheets[]` and the tile's COVER bit, never a constant. VTBAKE1's fixture was baked WITH height, so its tiles are 323,680 (2,983,034,880 / 9,216 at dim 2). Corrected text: "A tile is 138,720 B without ground cover and 184,960 with it on a default bake; 323,680 / 369,920 when the height sheet is carried (`--vt-height`); size it from the header (VT 3.3)." | VT 3.3 lines 2224-2229 table "no cover \| 46,240 \| 46,240 \| 46,240 \| 184,960 \| -- \| **323,680**"; VT 2.2 lines 879-881 "an all-BC1 tile is 138,720 bytes of colour classes and 184,960 of height"; src/io/lodvfile.cpp:237 `quint32 lodvTileRawBytes(` |
| 414 | "by the **seven** steps VT 2.5 states once" | STALE-NUMBER | VT 2.5 now lists **eight** steps; step 8 is the colour grade `colour *= grade` (default 1.0, branched over, so at the default steps 1-7 are the whole law; a runtime that ships a grade must read the bake's `landGrade`). Steps 1-7 as the plan paraphrases them (cell/quadrant, bilinear 17x17, base diffuse, ATXT in record order, <=0.001 skipped, VCLR multiply, tint after VCLR) all still match | VT 2.5 lines 1167-1179 "8  colour *= grade ... the bake census prints it as `landGrade`" |
| 421-422 | shore and wetness no longer baked (VT, the version-2 block) | OK | unchanged | VT lines 32-35 "**shore proximity and wetness are DROPPED.**" |
| 426-430 | diffuse sampled at `u = frac(wx/2048)`, mip `clamp(log2(max(1, unitsPerTexel / (2048/textureWidth))), 0, maxMip)`, trilinear (VT 2.5) | WRONG | The tiling is **T = 341.3333** world units (the engine's own landscape repeat, read out of Fallout4.exe 1.10.155), not 2048; the mip rule is `unitsPerTexel / (T/textureWidth)`. 2048 survives only as `--land-tiling 2048`, the way back to pre-2026-09-11 sheets. ALSO: the shipped default sampler is no longer plain trilinear -- source defaults are hex tiling 256, warp 341, mip bias -0.22, land guide flatwarp 1.0 (lane DEFAULTS1, 2026-09-12), and VT 2.5a says the runtime's ring 0 must use the same bake-wide sampler or seam. VT's own §5 CLI table still prints those four as off/0 (VT stale); VT 2.5j states the ruled default. Corrected text: "each source diffuse is sampled at `u = frac(wx/T)`, `v = frac(wy/T)`, T = 341.3333 (or the bake's `--land-tiling`), mip `clamp(log2(max(1, unitsPerTexel / (T/textureWidth))), 0, maxMip)`, through the SAME land sampler the bake used (hex / warp / mip bias / guide); VT 2.5, 2.5a, 2.5j" | VT 2.5 lines 1182-1190 "**T = 341.3333 world units, the engine's own landscape texture repeat**"; VT 2.5a line 1349 "**THE RING-0 CONSEQUENCE, for whoever writes the runtime.**"; src/lodgen.cpp:6277 `static float g_landTiling = 341.3333f;`, :6358 `g_landWarpAmp = 341.0f`, :6361 `g_landMipBias = -0.22f`, :6553 `g_landGuideRule = LODGEN_LANDGUIDE_FLATWARP;`, :6787 `g_landHexSize = 256.0f`; HANDOFF.md:1210 "land look = LAND1 panel (c) (hex 256, warp 341, mip bias -0.22, guide flat warp 1.0)" |
| 431-433 | pyramid NORTH-UP in table and payload (VT row-order block); `.lodl` row-0-SOUTH | OK | unchanged | VT lines 67-71 "`.lodt` is **NORTH-UP** -- in the tile table and inside every payload"; VT 3.3 line 2200 |
| 434-435 | mask = `rmaos`: R roughness, G metallic, B AO, A ground cover (VT 2.2); role 3 `data` RETIRED, refused by name | OK | unchanged | VT 2.2 line 917; VT 2.2 line 921 "Role **3 (`data`) is retired** and is refused by name"; src/io/lodvfile.cpp rule 13 "refused: sheet %1 has role 3 `data`" |
| 436-438 | coarse levels are downsamples (VT 2.3); only cover, albedo, roughness, metallic, emissive, height filter cleanly | OK | list matches | VT 2.3 lines 1061-1067 |
| 437-438 | "AO is a fixed **2,048-unit** horizon march" | STALE-NUMBER | VT 2.3 still says 2,048, but VT 2.2 now records that the march in BOTH sheet composites stops at **1,458** units (128 x 1.5^n up to 1,458; 2,048 is the loop bound, never sampled); only the per-vertex march reaches 2,048. The bake census prints `objAoReach 1458`. `mean(AO) != AO(mean)` still holds | VT 2.2 lines 952-958 "stops at 128, 192, 288, 432, 648, 972 and **1,458** -- 2,048 is the loop's bound" vs VT 2.3 line 1063 "AO is a fixed 2,048-unit horizon march" |
| 439-440 | index says `coarseLevelsAreDownsamples: true` | OK | unchanged | VT 4 JSON line 2402 |
| 441-443 | exactly ONE sheet may declare two formats, the cover carrier (VT 2.2, VT 3.4 rule 13) | OK | rule number and wording still hold; carrier may be role 5 or role 1 | VT 2.2 lines 971-977; VT 3.4 rule 13 lines 2311-2321; src/io/lodvfile.cpp "exactly one sheet carries the ground cover" |
| 445-448 | fourth sheet `R16_UNORM` height, `pixel = height/8 + 32767`, same encoding as the shadow heightmap (VT 2.2); opt-in `--vt-height` | OK | unchanged; default OFF in source | VT 2.2 lines 872-875; src/lodgen.h:1364 `bool height = false;` (struct LodgenVtOptions) |
| 448-449 | height "more than doubles a tile: 184,960 B of height against 138,720 of colour classes" | OK | matches | VT 2.2 lines 879-881 |
| 449-451 | a bake without it leaves R3 no height source beyond the inner band | OK (note) | still true of a bake without `--vt-height`; the VTBAKE1 fixture CARRIES the height sheet (4 sheets), so R2/R3 development has one | bake.log `vt:` "sheets 4"; lodt_check.txt "lodt sheet 3 role 4 dxgi 56" |
| 462-464 | arm refusals `no_lodm`, `no_lodt`, `refused_hard` (CENSUS 2) | not VT | out of scope (CENSUS page) | -- |
| 465-467 | `.btr` chunk sheets not deleted; the pyramid supplies their bytes, does not replace the files (VT 2.4) | OK (note) | VT 2.4 still says it. Since 2026-09-11 (VT 2.5b) the default `--land-detail-source vanilla` makes a chunk's `_msn` Bethesda's own file byte for byte and a paint-less chunk's colour vanilla's too, so the pyramid no longer supplies EVERY chunk-sheet byte; the files still stay | VT 2.4 lines 1136-1138 "**The chunk sheets are not deleted.**"; VT 2.5b lines 1378-1386 |
| 476-481 | ring-0 gate: independent impl, own ESM walk, own BC1/BC3/BC5U decoding; two Sanctuary tiles mean **3.26 / 3.59**, p95 **8**, max **14 / 17**, floor **13.70 / 15.15** (4.2x), ceiling **0** over **73,984** texels (VT 2.5) | OK | every number matches VT 2.5's table. Caveat: measured 2026-09-11 on a `--grass-tint 0` bake, i.e. before the 2026-09-12 sampler defaults (hex/warp/guide) -- the VT page records no re-run on the current defaults | VT 2.5 lines 1249-1252 "\| (-20, 24) \| 2,704 \| **3.26** \| 8 \| 14 \| **13.70** (4.2x) \| **0** over 73,984 texels \|" |
| 483-485 | grass tint from cover byte and `tintStrength` as VT 1.5 states it; mean **26.97/255** over the 256 highest-cover texels | OK | matches | VT 2.5 lines 1258-1261 "mean tint delta of **26.97/255** over the 256 highest-cover texels"; VT 1.5 lines 208-236 |
| 502-503 | must not alpha-test the colour sheet's alpha when cover is in the mask (VT 2.2a) | OK | unchanged | VT 2.2a lines 998-1003 |
| 504-505 | must not renormalise layer opacities for colour; cover side renormalises (VT 1.2) | OK | unchanged | VT 1.2 line 142 "**The renormalised `a_i` never reaches the colour composite.**" |
| 519, 575-577 | R3 reads the pyramid's height sheet / "the pyramid's resident height sheet beyond" | OK (conditional) | true only for a `--vt-height` bake; VTBAKE1's is one | as rows 445-451 |
| 953-957 | `.lodt` north-up, `.lodl` row-0-south; `ROW_ORDER_NORTH_UP` clear is a refusal (VT 3.4 rule 19) | OK | rule 19 unchanged | VT 3.4 line 2328; src/io/lodvfile.cpp:651 `// rule 19` "refused: flags bit 0 ROW_ORDER_NORTH_UP is clear" |
| 1027 | ring-0 colour error 3.26 / 3.59; floor 13.70 / 15.15; ceiling 0 (VT 2.5) | OK | as row 476-481 | VT 2.5 lines 1249-1252 |
| 1028 | "a pyramid tile: 323,680 B without cover, 369,920 B with" (VT 3.3) | WRONG | same defect as 410-412: that is the height-carrying tile. Corrected: "138,720 B without cover, 184,960 B with (default); 323,680 / 369,920 with `--vt-height`" | VT 3.3 lines 2224-2229; VT 2.2 lines 879-881 |
| 1058 (row 9) | far-terrain sheets DXT1 8 mips vs vanilla's 6,120 DXT5 10 mips; "A DXT1 `_msn` has no alpha, and our chunk renders blue-purple beside vanilla's"; owner TERRAINFMT1 | WRONG | TERRAINFMT1 is **CLOSED, 2026-09-12 13:45:27**: it shipped `--sheet-format vanilla` (DXT5, 10 mips, off by default -- `legacy` stays the default, so the DXT1/8 statement is still the default) and `--msn-cache`. It MEASURED that the blue-purple is **not a sheet**: it was the `.BTR`'s identity VERTEX COLOURS, and those are OFF by default since lane DEFAULTS1 (2026-09-12, bungo 15:53/15:56). Also by default (`--land-detail-source vanilla`) the chunk `_msn` IS vanilla's file. Corrected row: "closed 2026-09-12 (TERRAINFMT1 + DEFAULTS1): `--sheet-format vanilla` exists, default `legacy`; the blue-purple was the BTR vertex colours, off by default; nothing left for R2's fallback look from this row" | HANDOFF.md:1281 "LANDED 13:4x: TERRAINFMT1 DONE 13:45:27"; VT 7a.2 lines 2568-2576 "**The switch cannot change a colour.**"; VT 7a.4 lines 2612-2614 "Neither switch affects the blue-purple cast ... That cast is the terrain `.BTR`'s VERTEX COLOURS"; VT 7a.4 lines 2626-2633 (DEFAULTS1) |
| 1059 (row 10) | splat grading: vanilla graded "x0.82 uniformly and smoothed", ours raw; 19.96/255 on one chunk, NOT the roads; owner SPLAT1 | WRONG | SPLAT1 is finished: phase A 2026-09-11, phase B (the T = 341.3333 tiling -- the "smoothed" half) landed in RESUME3 2026-09-11. The grading half went to lane **GRADE1, DONE 2026-09-12 03:23**, which REFUTED a uniform grade: per-tile optimum gain runs 0.615..1.241 (straddles 1), the two reference tiles want 0.8916 and 1.1180, so no constant helps both; it shipped `--grade K` default 1.0 with "No value is recommended" and names the gap a per-cell CONTENT difference (which textures are blended where), owned by the composite, not by a grade. 19.96 and "not the roads" still stand (HANDOFF correction). Corrected row: "colour gap vs vanilla is a per-cell content difference, not a grade (GRADE1, 2026-09-12); `--grade` exists, default 1.0, no value recommended; owner: whichever lane owns the composite; still a look gap" | VT 2.5f lines 1608-1615 "**The gain has no single value.** ... per-tile optimum runs **0.615 .. 1.241**"; VT 2.5f "belongs to the lane that owns the composite, not to a grade"; HANDOFF.md:1113 "GRADE1 DONE 03:23"; HANDOFF.md:2937-2940 "**Chunk (-20,24) contains zero road triangles**" |
| 1061 (row 12) | height sheet opt-in; "the CLI table (VT 5) does not list the flag although VT 2.2 names it" | WRONG | VT 5 now LISTS `--vt-height`, default **off**, with the byte costs; the CLI help lists it too (HANDOFF.md:94). Default in source is still off. The "bungo for the bake" half: VTBAKE1 baked the Commonwealth fixture WITH `--vt-height` (per its brief), but no ruling on the shipped bake was found. Small VT defect: the VT 5 row cites "§3.6", which does not exist (the figures are in §3.3). Corrected row: "the height sheet is opt-in (`--vt-height`, default off, listed in VT 5 and the CLI help); the VTBAKE1 fixture carries it; whether bungo's full bake does is his call (§6 e)" | VT 5 line 2448 "\| `--vt-height` \| **off** \|"; src/nifcli.cpp:7121 `[--vt-height]`, :8044 `lgVt.height = true`; HANDOFF.md:94 "The height layer for the far terrain is now listed in the command-line help"; scratchpad/brief_vtbake1.md "Bake Commonwealth with --vt (plus --vt-height, row 12)" |
| 1063 (row 14) | "no `.lodt` container and no VT `.lodm` has ever been written to disk" | WRONG | written 2026-09-23 08:17 by VTBAKE1 (scratch tree, see row 89); VTBAKE1 is LIVE, its gates not finished (dim 2 passed `--lodt-check` rc=0 in 8m14s; dim 4 running at 08:28). Corrected row: "first whole-Commonwealth pyramid written 2026-09-23 by VTBAKE1 into scratchpad/vtbake1_20260923/bake (height on, cover off); validation in progress; not in the mod folder" | scratchpad/vtbake1_20260923/lodt_check.txt "== dim 2 ... rc=0 == dim 4"; HANDOFF.md:371 "VTBAKE1 is LIVE too (08:0x)" |
| 1126-1129 (§6 e) | must the full bake run with `--vt-height`? 184,960 vs 138,720 of the three colour-class sheets | OK | numbers match VT 2.2; question still open (no ruling found); VTBAKE1 used it for the fixture only | VT 2.2 lines 879-881 |
| 1156-1162 (§6 j) | cover in mask (shipped) or colour (`--vt-cover-in-color`); identical bytes, measured; 2,001 of 2,001 vanilla chunk colour sheets DXT5; discriminator = meaning (VT 2.2a); keep the mask | OK | unchanged; still "bungo's call ... open" in VT 2.2a. (VT 7a.1 now also has the wider count: all 6,120 Commonwealth sheets, 3,060 colour, DXT5.) | VT 2.2a lines 993-1006 "**2,001 of 2,001** ... **This is bungo's call to make, and it is open**"; VT 7a.1 line 2539 |
| 1198 (glossary level) | `.lodt` `levelIndex` 0 = the FINEST level | OK | unchanged | VT 3.1 line 2131 "`levelIndex` \| 0 = the finest level of the set" |
| 1199 (glossary mask) | `.lodt` role 5 `rmaos`, R rough, G metal, B AO, A cover | OK | unchanged | VT 2.2 line 917; src/io/lodvfile.h:101 `LODV_ROLE_MASK = 5` |
| 1205 (glossary height) | `.lodt` role-4 sheet, `height/8 + 32767` | OK | unchanged | src/io/lodvfile.h:100 `LODV_ROLE_HEIGHT = 4, //!< R16_UNORM, height/8 + 32767` |
| 1248 (§8 version row) | `.lodt` version **2**, magic `LDTX`; refuse version 1 BY NAME; `LODT` and retired `LODV` each named | OK, but incomplete -- R0-IMPACT | Writer version 2 (`LODTEX_VERSION = 2`, `LODV_VERSION = LODTEX_VERSION`); the reader accepts ONLY 2; refuses version 1 by name ("refused: version 1 container -- four sheets whose role 3 is `data`..."), any other version generically ("refused: version %1, this reader knows %2"); magic `LDTX` = 0x5854444C; refuses `LODT` (landscape) and `LODV` (0x56444F4C) by name, anything else "magic is not LDTX". MISSING from the row: the tree's reader accepts **sheetCount 1..10** and **role 7 (horizon, RETIRED producer, reader-only)**: at most 4 role-7 sheets, R8G8B8A8 (DXGI 28), contiguous and last. VT 3.4 rule 13's TEXT still says "sheetCount outside 1..6 ... role > 6" is a refusal, so VT 3.4 and the source disagree; VT 3.1 (0x79 "3 to 9", 0xA0 "ten descriptors") and VT 3.5 agree with the source. The shipped exe writes no role-7 sheet. No ruling on which the FO4CS loader follows was found. | src/io/lodvfile.h:67 `LODTEX_MAGIC = 0x5854444CU`, :69 `LODTEX_MAGIC_RETIRED_LODV = 0x56444F4CU`, :91 `LODTEX_VERSION = 2;`, :118 `LODV_ROLE_HORIZON = 7`, :124 `LODV_HORIZON_MAX_SHEETS = 4;`, :137 `LODV_MAX_SHEETS = 10;`; src/io/lodvfile.cpp:29 `LODV_VERSION = LODTEX_VERSION`, :454-486 rules 2-3, rule 13 block `fd.sheetCount > LODV_MAX_SHEETS` / `s.role > LODV_ROLE_HORIZON`; VT 3.4 line 2311 "13. `sheetCount` outside 1..6 ... `role > 6`"; VT 3.5 lines 2272-2279 "**What the READER still does, and this is deliberate.**" |
| 1417-1439 (§8.5) | pyramid carries what a clipmap needs (aligned tile grid, `worldUnitsPerTile` and `contentTexels` stated per level) and deliberately nothing camera-relative (VT "Nothing clipmap-specific") | OK (quote accurate, one omission) | VT 6 still says it; its own list of "what a clipmap needs and gets here" is **the height sheet, one aligned grid, and the level ratios stated** -- the plan's paraphrase drops the height sheet (which is opt-in). Per-level `worldUnitsPerTile` / `contentTexels` confirmed in VT 4 | VT 6 lines 2482-2484 "* **Nothing clipmap-specific.** No camera-relative data, no toroidal layout, no morph bands. What a clipmap needs and gets here is the height sheet, one aligned grid, and the level ratios stated rather than implied."; VT 4 lines 2414-2416 "a consumer picking clipmap rings reads those two numbers at load time" |
| 1437-1439 (§8.5 unblocker) | "no `.lodt` pyramid has been written for a whole worldspace; R2 needs one Commonwealth `--vt` bake" | STALE | written 2026-09-23 08:17 (VTBAKE1), gates in progress, not yet DONE | as row 1063 |

## Section checks (the ones the brief listed)

* VT section numbers the plan cites -- 1.2, 1.5, 2.2, 2.2a, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 4, 5, 7 -- all still exist and still hold the cited content (except the rows above). None moved. New neighbours: 2.5a-2.5j, 3.5 (role 7, placed BEFORE 3.4 in the file), 7a.
* VT 3.4 refusal list: rules 1-22 plus 16b = 23 entries ("22 rules" in the VTBAKE1 brief counts 16b as part of 16). Rule 3 = version, rule 13 = sheets/roles/cover carrier, rule 19 = row order: unchanged numbers; source tags `// rule N` agree.
* `--vt-height`: default OFF (src/lodgen.h:1364); VT 5 lists it now.
* Grass tint 26.97/255, 73,984 texels, 3.26/3.59, 13.70/15.15, p95 8, max 14/17: all present verbatim in VT 2.5.
* Role 3 `data` retired and refused by name; role 7 horizon retired as a producer 2026-09-19 (lane HORIZONOUT) but still ACCEPTED by the reader.

## VTBAKE1 status (one line)

LIVE, not DONE: HANDOFF.md:372 "QUEUED 08:0x 2026-09-23 VTBAKE1: first Commonwealth --vt pyramid into a scratch tree" and HANDOFF.md:371 "VTBAKE1 is LIVE too (08:0x)"; on disk the bake finished 2026-09-23 08:18:04 rc=0 (4,022,886,880 B of tiles, 5 levels, height on, cover off) and `--lodt-check` passed dim 2, dim 4 running at 08:28.
