# PLANSYNC1 progress -- 2026-09-23 (clock 08:11)

- 08:11 started. Read brief, CONSTITUTION.md, skill ww-contract-provenance.
- stamp at start: docs/FO4CS_IMPROVED_LOD_PLAN.md sha256 bea1a2cad02b6331, 102,578 bytes, 1,605 lines, CR=0 (LF).
- source stamps at start: lodofile.h 44b11f9ebe0ea0cb/584; lodofile.cpp f7c78706f0d6d8d0/2231; lodifile.h 5d993d1ae8eebe8d/798; lodifile.cpp f0a608321df133f5/2282; lodtfile.cpp 6809fe6576ff84eb/3664; lodtfile.h c61cc41ea8a75f6e/436. (no src/lodmfile.*; .lodm writer located later)
- 08:14 read plan in full (1605 lines). Two sections both numbered 8 (Provenance, and Cell Manager 8..8.5) -- noted.
- 08:20 VERSIONS from source (writer constants + reader accept lists):
  - .lodo: LODO_VERSION 4 (src/lodofile.h `constexpr quint32 LODO_VERSION = 4;`); reader refuses 1, 2, 3 BY NAME (3 = base row reinterpreted: crossPx16[0..1] -> u32 fullTriangles), anything else "version %1; this reader knows 4". v5 existed only in lane HORIZON3 and was never written by any exe.
  - .lodi: writer version is CONDITIONAL on modules: 3 base / 4 aggregate / 5 placement AO / 6 vertex AO / 7 group table + vertex-sky stream (header block 512 B) / 8 RETIRED horizon (reader-only) / 9 = v7 layout + instance flag bit 6 scrappable. Reader accepts 3,4,5,6,7,8,9; refuses 1,2 by name.
  - DEFAULT bake (src/nifcli.cpp): placementAo=1, vertexAo=1, lodiV7=1, scrappable=0 -> a default .lodi is VERSION 7 (512-byte header), not 3.
- **R0-IMPACT 1 (08:20)**: a default bake's .lodi is v7 with a 512-byte header block; headerCrc32 covers 0x10..lodiHeaderBytes(version)-1 (=0x1FF on v7/v9, 0xFF on v3..v6). A loader pinned to "version 3" or a 256-byte crc window refuses/fails every fresh file. R0 must accept 3..7 and 9, tolerate 8 (retired, parse + skip), refuse 1/2 by name, and size the crc window by version.
- **R0-IMPACT 2 (08:20)**: bungo 2026-09-17 "Authored LODs only" -> CLI default is NOW `--native-no-ladder` (LODO_FLAG_LADDER clear, levelMax 0, every geometricError 0) and `--library mnam` (src/nifcli.cpp `bool lgNativeLadder = false` + `bool lgLibraryNear = false`). The plan's §6(a) "`--library near` is the default" and R0 gate 1's `levelMax 7` / 20,678 clusters are STALE for a default bake. R0's gate numbers must be re-pinned from a current file; R3's cut serves `no_ladder` (level 0 only) on every default bake.
- **R0-IMPACT 3 (08:31)**: with authored LODs only, the library's four levels ARE the four MNAM slots (`rep[0..3]`); each instance was baked from ONE slot (`mnamSlot`, counted into `.lodi` 0xD4 `slotInstances[4]`) but mnamSlot is NOT stored per instance in the file. The v6 vertex-AO and v7 vertex-sky slices are sized to `rep[mnamSlot]`'s vertex count, so a loader can only attach a slice to a mesh by LENGTH match (the NifSkope viewer does exactly that and counts `vaoMismatch`, src/lodinative.cpp). R0's loader must not assume slice == rep[0]; a slot drawn other than the baked one has no per-vertex AO. Candidate owed row for the generator (store the slot, or a slice per slot) -- NOT ruled.
- **R1 note (08:31)**: WW_CHANGES 2026-09-17 hotfix 2 states a reader-contract rule: mirror texture U about the SHAPE / material U range, never the whole mesh (TreeMapleForest2 bark-on-branches). Goes into R1 DOES.
- 08:36 launched 4 read-only audit agents (Opus): VT/R2 -> audit_vt.md; cards/R4 -> audit_cards.md; BTD+CENSUS+FO4CS side -> audit_btd_census.md; NATIVE -> audit_native.md. A disk-wide find for LOD containers is running in background.
- 08:40 HANDOFF top block read (lines 360-420). Card items for R4 / §6:
  - RULED 04:5x 2026-09-23 (HANDOFF, IMPOSTORRING1 HELD line): "for fo4cs use the convention was 22.5 degrees per take" -> tree cards = 16 azimuths at 22.5 deg on the horizon ring, aggregate ring layout reused, 2-frame blend; a raised second row = his call. Lane IMPOSTORRING1 HELD behind IMPOSTOR16. THIS CONFLICTS with R4's "N x N hemi-octahedral, three-frame blend" -- both must be stated; which layout FO4CS reads is not yet reconciled in the spec (to verify against IMPOSTOR_SPEC via the cards audit).
  - IMPOSTORFIN1 2026-09-22: vanilla alpha cut 128/255 is now the default.
  - RULED 07:5x 2026-09-23 IMPOSTORPBRM1 scope: cards MUST carry .pbrm specular weight + colour + IOR and the TintMask (4 channels) applied in the bake. QUEUED.
  - QUEUED IMPOSTORWIND1 (sway from the tree model's own wind weights; research first, sheet layout for his ruling).
  - OPEN: grid + cut default (N4/N8/N16; stipple / average / strongest cut) after IMPOSTOR16 N8; gloss/AO default; `_n` height<->sway swap.

- 08:36 audits in: VT (28 OK / 9 WRONG / 2 stale-number / 1 stale), CARDS (20 OK / 7 STALE / 4 WRONG), BTD partial, NATIVE partial.
- 08:36 R0-IMPACT 4: `.lodl` -- FO4CS R0 brief says "accept v2 and v3; v1 named" but our reader ACCEPTS v1 and WW_LODL_VERSION=1 is the documented way back; refusing v1 kills that fallback. Not ruled. Also: `.lodl` ROW 0 IS SOUTH (BTD 220), plan never says so; a north-up reader must mirror Y. Shipped FO4CS parser opens Data\Terrain\<EDID>.lodt (old name + old folder), not Data\FO4CSLOD\<ws>\<ws>.lodl.
- 08:36 R0-IMPACT 5: `.lodt` (LDTX v2) reader also accepts up to 10 sheets and role 7 (retired horizon sheet, max 4, RGBA8, last); VT 3.4 rule 13 still says 1-6 sheets. Tile bytes must come from the header sheet list + cover bit (lodvTileRawBytes), never a constant: default tile 138,720 B (184,960 with cover); 323,680/369,920 only with --vt-height.
- 08:36 R0-IMPACT 6: cards -- `.lodm` carries `conv` ("spec1") on card + each layer since 2026-09-19; absent = pre-repair set turned 180 deg, re-bake owed; loader must read it. Not in LODM 3. `kind: "aggregate"` exists (CARDS-AGG 2026-09-11), so `not_baked` is not the only arm. Coverage cut domain conflict: plan/contract encoded test 128 vs viewer decoded 128/255 -- unruled.
- 08:39 R0-IMPACT 7 (NATIVE audit): `.lodo` refuses v3 by name too (v4 only). A mismatch BETWEEN the two files (worldspace, plugin/object corpus hash, loadOrderHash, lodoIdentity unless NOLIB) is a HARD pairing refusal; only a mismatch against the RUNNING game is soft. Full reader hard list in audit_native.md part 4 goes into R0. `cardCount` is bounded only, never recounted -- loader should recount. CENSUS 2 `version` field still reads `lodo3/lodi3` and has no `.lodl`/`.lodt` slot (census page stale; finding).
- 08:39 gate-1 re-measure (NATIVE audit, bytes on disk): default authored `.lodo` 6,204,388 B, v4 flags 0x5, 2,970 bases, 2,982 meshes, 10,634 clusters, levelMax 0, 10,634 roots / 142,138 L0 tris, CONE_OPEN 6,982, WATERTIGHT 365, fullTriangles max 2,706. No default Sanctuary `.lodi` on disk -> `.lodi` size gate owed a re-bake.
- 08:43 all four audits in; writing the splice script now (scratch session dir), plan still at start stamp bea1a2cad02b6331.
- 2026-09-23 08:51 splice applied: 80 replacements, plan 102,578 -> 144,089 B, 1,605 -> 2,119 lines, CR 0, sha16 d81962d25167739f. Next: DELIVERABLE_TEXT, anchors, re-hash.
- 2026-09-23 08:52 provenance: every new source anchor found in exactly one place. `bool lgNativeLadder = false;` was not found as written; it is re-anchored as the full declaration. The mnamSlot owner is corrected to LodiSrcInstance. The §9 cites lodifile.h:556/490/412 still hold. Version constants were re-read: LODO 4, LODI 3..9, LODL 3/min 1, lodt header 2, LODM 1. All 22 stamp rows (10 pages, 12 sources) re-hashed after the last edit and 0 moved. Plan final sha16 d81962d25167739f, 144,089 B, 2,119 lines, CR 0.
- 2026-09-23 08:52 DELIVERABLE_TEXT.md written: 27 contract-page defects, 2 stale source strings, and a changelog line for the overseer.
DONE PLANSYNC1 2026-09-23 08:52 -- plan re-synced (61 corrections), not committed; no other page, source, HANDOFF, WW_CHANGES or MISTAKES touched.
