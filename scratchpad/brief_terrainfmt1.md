# Lane TERRAINFMT1 -- the far-terrain sheets in vanilla's format (DXT5, 10 mips) and the normal sheet's texel-scale detail

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`. Nothing is committed. Exe at launch: `release/NifSkope.exe` 2026-09-12 11:51:35, 22,000,128 B, sha1 ecf5ecab537f70a3409f2df3da4f4bbda2b08708 (GROUND1's: carries --terrain-object-ao, --erosion, --land-guide, --incremental, and the UINOTES2 UI merge). Rung ONCE: `release/NifSkope.before_terrainfmt1.exe`. Markers `scratchpad/terrainfmt1_20260912/BUILDING` / `DONE` (2026-09-12 dir; every path in this brief that says terrainfmt1_20260911 means terrainfmt1_20260912).
- Baselines on that exe (GROUND1's chain 12:2x, all nine sequential, zero NifSkope before each): lodgen_roads 11/0, lodgen_native 18/0, lodgen_native_baseline 25/25 0 differ, lodgen_terrain_vt 41/1 (V9c, inherited), lodgen_ground_cover 29/5 (inherited; compare by check NAME), lod_generation 116/0, lodl_open 23/0, lodgen_terrain_pbrm 14/0, animws 236/0/1 skip. Same rows expected from you at those numbers, plus your own gates.
- Build rules learned tonight: after every build run `make -n` in the MSYS2 shell and require nothing left to compile (exe-newer-than-source passed over six stale objects for LAND1); check .o staleness against every header you touch WITHOUT touching source mtimes (GROUND1 bumped two by accident). Count Fallout4.exe and NifSkope.exe SEPARATELY (two tasklist lines, two numbers) before every build and every exe launch. Never touch E:/Projects/NifskopeWWE_ui, scratchpad/land1_20260912/, scratchpad/ground1_20260912/, scratchpad/uinotes2_20260912/, src/anim*, src/ui/**, res/**.
- ADDED ITEM 8 (bungo's cleaned 2K _msn cache, director 12:3x): `E:/Tools/Upscale/esrgan-bat/output/` holds 2,304 PNGs, 2048x2048 RGB, named `Commonwealth.4.<x>.<y>.png` -- vanilla's dim-4 `_msn` sheets run through his chain (1x BC1 block-artifact cleaner -> blue zeroed -> 4x ESRGAN normal-map model, blue left as the model wrote it). This is his chosen replacement for the copied vanilla `_msn` on chunks that reuse vanilla's sheet (the default `--land-detail-source vanilla`). Do, after items 1-4: (a) MEASURE the cache before anything ships: per channel means, unit length after renormalising (which channel is derived from the other two follows from item 1's channel semantics -- state it; the HANDOFF note of 23:3x says recompute G from R,B; his bat says blue was rebuilt -- measure which is consistent with vanilla's 512 sheet), coarse agreement with vanilla's own 512 sheet (downsample 4x, per-channel correlation, floor = a phase-randomised twin), fine energy at the 4-texel scale vs vanilla and vs the twin, block-grid residue (the 4-texel BC1 grid must be GONE: measure its spectral line before/after); (b) ingest behind `--msn-cache <dir>` (default off = rung bytes): for a chunk with a cache file, the `_msn` written is the cache sheet, renormalised, in BC7 or uncompressed (item 1's law decides; DXT5 re-encode brings the blocks back -- prove that with one sheet: encode DXT5, measure the grid line returning) with a full mip chain, alpha per item 1; chunks without a cache file take the existing path; (c) the render-hook pair on chunk -20,24 (his judged chunk): vanilla 512 | ours with the cache, same camera; (d) size and load: 2048^2 BC7 = 4 MB per sheet x 2,304 = about 9.4 GB for the Commonwealth -- report it beside vanilla's total and say what mip-0 dropping (1K) would cost in the same numbers; his call, do not decide it. Gate F5: cache off == rung bytes; on: grid line gone (number vs before), unit length within 1/255, coarse correlation vs vanilla above the twin floor per channel. Picture `cmp_msn_cache.png` (vanilla 512 | cleaned 2K | difference x8 | the render pair). Every timestamp from `date +%H:%M`. One NifSkope instance; game check before the link; bungo's window renamed aside. Region bakes only, own out-dir.
- Read first: `CONSTITUTION.md`; `HANDOFF.md` top block -- CLOCK CORRECTION, the ROADS1 block's red 2 (our sheets DXT1 / 8 mips vs ALL 6,120 of Bethesda's DXT5 / 10 mips; "a DXT1 _msn has no alpha channel -- this is why our chunk renders blue-purple beside vanilla's"), the PIC-CHUNK paragraph (our _msn roughness 0.93 vs vanilla's 10.15 at texel scale; candidate source = the land textures' own normal maps blended by the same weights), the TERRAIN-R block (sheet formats, the BC3-when-cover rule and the 'WWCV' stamp), the GRADE1 and RESUME3 blocks (the colour law you now bake with); `docs/LODGEN_TERRAIN_VT.md` 1.4 (the stamp), 2.2 (formats), 2.3 (the filter), 2.4 (the chunk sheets); `docs/LODGEN_PARITY.md`; `scratchpad/pic_chunk_20260911/NOTES.md` + `facts.json`; `scratchpad/lane_roads1_report.md` section 1.4 (the `_msn` measurement: vanilla's normal agrees with the bare LAND heightmap BETTER on the road -- so whatever detail vanilla adds is NOT the road mesh); `src/lodgen.cpp` `lodgenTerrainMsnPixel`, `lodgenTerrainHeightAt`, the DDS writers (`lodgenWriteDds*`, mip count, BC1/BC3 selection); the game's `_msn` semantics: what does the engine read in the `_msn` alpha? (measure vanilla's alpha channel: constant? a mask? -- state it before deciding what to write there); `MISTAKES.md` root from 2026-09-11.
- Skills: `nifskope-ww-vanilla-compare`, `ww-control-calibration` (the detail metric needs a floor: a phase-randomised twin of vanilla's _msn, and a ceiling: vanilla vs itself), `ww-sheet-diff`, `ww-texel-picture`, `nifskope-ww-render-shot` (the blue-purple render beside vanilla's, before/after, same camera -- proof by picture), `ww-spec-gate-audit`, `fo4cs-census-field`, `nifskope-ww-lodgen`, `ww-anchored-hookup`, `nifskope-ww-build-verify`, `ww-contract-provenance`.

## The work
1. **Measure vanilla's format law first**: over all 6,120 shipped Commonwealth sheets (ROADS1 counted them): fourCC, mip count, and the `_msn` ALPHA channel's content (constant 255? a height? a mask?) -- histogram over a sample of 50 sheets; the colour sheet's alpha likewise. State what the engine can be reading from each alpha (cite the shader if the tree knows: FO4CS's terrain LOD shader reads `_msn` how? -- read-only look at E:\Projects\Fo4CommunityShaders\wt-fixfirst for the LOD terrain shader's _msn sampling, quote the lines).
2. **Format**: write DXT5 with 10 mips for colour and `_msn` on the STOCK path (the pyramid keeps its own contract unless the same alpha semantics apply -- state it), with the alpha channel carrying what item 1 says vanilla carries; the 'WWCV' stamp rule for cover survives (the cover-bearing sheet is DXT5 anyway now -- reconcile the stamp: cover in alpha vs vanilla's alpha semantics; if they collide, the stamp decides and the report says so). Behind `--sheet-format vanilla|legacy` with `legacy` = the rung's bytes.
3. **The render-hook proof**: the blue-purple defect -- render our chunk beside vanilla's through the render hook, same camera, before and after (`nifskope-ww-vanilla-compare`); the after must not read blue-purple (a colour-distance number on the terrain pixels vs vanilla's render, floor = before).
4. **The _msn detail term**: measure the candidate: blend the land textures' own `_n` maps (footprint mip, same weights, same tiling 341.333) into the height normal (state the composition: detail normal in tangent space rotated onto the height normal, or a simpler additive with the strength fitted); metric = the texel-scale roughness PIC-CHUNK used, ours vs vanilla vs the twin floor vs the ceiling, on both tiles; fit the strength; ship it behind `--msn-detail <strength>` with 0 = rung bytes. If the candidate does NOT reproduce vanilla's detail (wrong spectrum, or the correlation with the blended `_n` maps is inside the twin floor), refuse with the numbers -- a refused round is a deliverable.
5. Build, then the chain at the baselines from TERRAIN-AO1's block; both switches at their off values == rung bytes (cmp every file); `lodl_open.sh` / `lodgen_terrain.sh` / `lodgen_terrain_vt.sh` / `lodgen_roads.sh` / `lodgen_ground_cover.sh` / `lodgen_terrain_pbrm.sh`; the pyramid contract's tests; the stock-engine tolerance gate (a DXT5 sheet with 10 mips is what the engine ships, so this should be the safest change of the night -- say so with the count).
6. Pictures: `cmp_render_msn.png` (render hook: vanilla | ours before | ours after), `cmp_msn_detail.png` (texel picture: vanilla _msn | ours before | ours after | blended _n candidate, roughness numbers burned in).
7. Documents: `scratchpad/terrainfmt1_20260911/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md` (entries start with `## `); report `scratchpad/lane_terrainfmt1_report.md`; contract amendments with provenance.

## Gates
- F1 vanilla's format law measured over the corpus before code (fourCC, mips, alpha content).
- F2 switches off == rung bytes; on: DXT5/10 mips and the render no longer blue-purple (number vs floor).
- F3 detail metric with floor and ceiling; strength fitted or refused with numbers.
- F4 chain at baseline; exe newer than every changed file; drivers rebuilt; rung == launch bytes; no NifSkope left running.

## Rules
- One build (+ counted relinks). No tone changes (GRADE1's), no tiling changes. Never his installed files. Never `git stash`, never commit. Plain language.

## Report
`scratchpad/lane_terrainfmt1_report.md`, incremental (PENDING.md first past half context): `## 0. Pre-registered gates`, `## 1. Vanilla's format law`, `## 2. The format change and the render proof`, `## 3. The detail term`, `## 4. Build and gates`, `## 5. Pictures`, `## 6. Owed / red / bungo's calls`, `## 7. Mistakes`, `## 8. Finished-work skill review`.
