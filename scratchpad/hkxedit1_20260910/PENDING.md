# Lane HKXEDIT1 -- BUILD PENDING resume (nifskope-ww-resume-pending)

State on disk, 2026-09-10 ~16:1x: every deliverable is written and syntax-
checked with the real flags (`scratchpad/hkxedit1_20260910/sx.sh`,
SYNTAX-RC=0 on `src/hkxfile.cpp`, `src/hkxmodel.cpp`, `src/hkxmodeltest.cpp`,
`tests/hkxfile_gate.cpp`); the standalone gate binary is built and green
(`release/hkxfile_gate.exe`, `python tests/spells/hkxfile_gates.py` = 109
checks, 0 failures); NifSkope.exe was NOT relinked (the brief: never launch
it, build nothing) and the hook-up is NOT applied (four files held by other
lanes). Nothing committed.

## Resume, in order

1. `tasklist | grep -i -E "Fallout4|NifSkope"; echo rc=$?` -> rc=1, or stop.
2. `python scratchpad/hkxedit1_20260910/hookup.py` (the --check): expect all
   ten anchors `count=1` (NifSkope.pro x3, src/nifskope.h x2,
   src/nifskope.cpp x4 CRLF, src/nifskope_ui.cpp x1). Then `--apply`; it
   asserts every file's CR count unchanged. The inserted text carries
   "(lane HKXEDIT1)" markers.
3. **qmake before make** (three new SOURCES + two HEADERS + the JSON copy
   line in NifSkope.pro): `qmake NifSkope.pro` then `make -j2` per
   `nifskope-ww-build-verify`; read `GeneratedFiles/.obj/hkxmodel.o`'s
   dependency block back and confirm it names `src/hkxmodel.h`,
   `src/hkxfile.h`, `src/hkxanim.h`. Confirm `release/hkclasses_fo4.json`
   exists after the link (the copyFiles line).
4. The exe-newer sweep over `src/hkxfile.*`, `src/hkxmodel.*`,
   `src/hkxmodeltest.cpp`, `src/nifskope.cpp`, `src/nifskope.h`,
   `src/nifskope_ui.cpp`.
5. `bash tests/spells/hkxmodel_test.sh` -- the WW_HKXMODEL_TEST harness stub,
   NEVER RUN by this lane: pre-registered 20-odd checks, expecting PASS on
   jog.hkx: (a) the tree's model is an HkxModel with 6 blocks in file order,
   (b) numFrames 23 -> 24 through setData, undo 23, redo 24, stack clean ->
   dirty, (c) unedited save byte-identical to jog.hkx, edited save differs
   inside the animation object and reloads with numFrames 24, (d) animFile()
   decodes a 23-frame clip. Its own defects are found by this first run
   (ww-test-harness-add section 9). Absolute Windows paths (skill section 12).
6. Re-run `python tests/spells/hkxfile_gates.py` (rebuild the gate binary
   first only if hkxfile.cpp changed: `bash scratchpad/hkxedit1_20260910/build_gate.sh`).
7. The four documents: WW_CHANGES.md (splice
   `scratchpad/hkxedit1_20260910/WW_CHANGES_ENTRY.md`, mixed file, CR count
   unchanged), MISTAKES.md (this lane's entries were appended by the lane),
   `scratchpad/lane_hkxedit1_report.md` (append a `## Build` section), and
   the `ww-hkx-animation` skill section 13 (appended by the lane in the repo
   tree; the director applies it to the live tree).

## What a resume may find, and what it means

* `hookup.py --check` counting 0 on a nifskope.cpp anchor: another lane's
  patch moved the region or changed its ending; re-probe the bytes with
  Python (`MISTAKES.md` 2026-09-10 entry 2), never with `cat -A`.
* The Blocks tab empty after opening an .hkx: `HkxModel::loadError()` names
  the reason; the most likely is "no class database found; tried ..." -- the
  JSON did not get copied beside the exe (step 3's last check) and
  `WW_HKCLASSDB=E:/Projects/NifskopeWildWastelandEdition/res/hkclasses_fo4.json`
  is the zero-effort fallback.
* Undo through Edit > Undo not reaching the .hkx document: expected --
  the window's actions are created from `nif->undoStack`
  (`src/nifskope_ui.cpp:23693`); a `QUndoGroup` over both stacks is owed
  (report section 6).
* The census set `scratchpad/hkxedit1_20260910/census_hkx/` (15,320 files,
  gitignored) is regenerated by `extract_census.py` in 13 s if absent.
