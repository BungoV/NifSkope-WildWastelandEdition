# Lane CARDS-AGG -- aggregate impostors at ring 3: one card set per forested cell, the ring-3 instance list one per cell

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`. Nothing is committed. Exe at launch: `release/NifSkope.exe` 2026-09-11 14:48:52, 21,261,312 B (BAKEPERF1). Since this brief was drafted: NATIVE1a/1b landed (`.lodo`/`.lodi` v3, the reserved room per section 12 of the contract has been partly USED by 1b -- read the v3 contract for what is still free before choosing where the aggregate table goes), LODUI1 landed (Trees-only row, the native row, `lodgen_panel_run.sh`), BAKEPERF1 landed (`src/lodgenchunkpass.{h,cpp}` is THE chunk queue now, results retire in job order; the headless path suppresses crash dialogs; `--threads`/`--chunk-threads`). Read their blocks in HANDOFF.md. Baselines from BAKEPERF1's block: lodgen_native 18/0, lodgen_panel_run 125/0, lod_generation 116/0, lodgen_terrain_vt 41/1 (pre-existing red). Rollback rung, ONCE: `release/NifSkope.before_cards_agg.exe`.
- Read first: `CONSTITUTION.md`; `HANDOFF.md` top block -- RESUME, the newest lane block, the rulings of 2026-09-11 08:2x-08:3x (aggregate ring-3 impostors: "1 sounds good"; the far-shadow constraint: cards cast from the height channel, aggregate sheets carry height too; the identity gate: identity unique per placement, and on the aggregate sheets), 07:0x (trees only; the three-test tree set), NATIVE1a/1b blocks (the `.lodi` record, the ring/cell-range blob, the reserved room), and the screen-size fade spec; `docs/LODGEN_IMPOSTOR_SPEC.md` IN FULL and `docs/LODGEN_CARD_SHEETS.md` IN FULL (the octahedral bake, hemi-octahedral mapping, frame law, gutters, channels incl. height in normal B and sway, the card `.lodm`, the repetition breaking); `docs/LODGEN_LODM_FORMAT.md` (a new `kind` needs its section); `src/nifskope_ui.cpp` ~22518 (`WW_IMPOSTOR_OCT`/`WW_IMPOSTOR_TILE`, the bake hook), `tools/bake_impostor_cards.sh`, `src/lodgen.cpp` `lodgenCard()` / `lodgenBuildCardArrays()` and the `isTree` block (~3318, the position-stable hash for rotation and mirror); `MISTAKES.md` root from 2026-09-11.
- Skills you MUST invoke: `nifskope-ww-render-shot` (the aggregate bake IS a render through the hook; one instance; invisible; second monitor), `ww-texel-picture`, `ww-control-calibration` (the aggregate-vs-individual comparison needs a floor and a ceiling), `ww-standalone-writer-gate` (the `.lodi` aggregate table / the new `.lodm` kind), `ww-contract-provenance`, `fo4cs-census-field`, `ww-anchored-hookup`, `nifskope-ww-build-verify`, `nifskope-ww-lodgen`.
- Build rules: ONE build (+ counted relinks). Markers `scratchpad/cards_agg_20260911/BUILDING` / `DONE`. Game check before the link; bungo's window renamed aside. `ls scratchpad/*/BUILDING` empty at launch. Region bakes only, own out-dir.

## bungo's rulings (verbatim in HANDOFF.md)
- Aggregate ring-3 impostors accepted: "At ring 3 the bake takes each cell's trees, places their cards with the same rotation and mirror the repetition breaking would give them, photographs the whole cluster from the horizon views, and writes one aggregate sheet per cell. The ring 3 instance list then holds one placement per cell instead of one per tree. At the ring 2 to 3 border the per-tree cards cross-fade into the cell card. Same sheet format, same sway rule from the height channel." Count of forested cells printed before anything is built.
- Trees only. Far shadows: the aggregate sheets carry height. Identity: present on the aggregate sheets (the cell's identity -- state the law: one identity per aggregate, or the dominant tree's, and why).

## The work
1. **Census first**: for the region and (by the ESM scan alone, no bake) for the whole Commonwealth: forested cells at ring 3 (cells with >= N tree placements; state N, propose 8), trees per such cell (histogram), the sheet count and bytes the aggregate would cost at the frame size you choose (horizon views only: propose 8 azimuths x 1 elevation band, or the hemi-octahedral grid's bottom row -- state the trade). Printed BEFORE the code; the report's section 1 is timestamped before section 2.
2. **The aggregate bake**: per forested cell, compose the cell's tree cards (their sheets, at the per-tree rotation/mirror from the position-stable hash) or the tree LOD meshes (state which and why -- cards of cards compound the frame quantisation; meshes are exact) in a headless scene, photograph from the horizon views through the existing two-pass matte hook, write ONE card set per cell: colour+coverage, normal+height+sway, mask, with the same gutters and frame law; `.lodm` `kind: "aggregate"` with the cell, the tree count, the identity law, `card.projection: ortho`.
3. **The instance list**: under `--aggregate` (module switch, off = byte-identical), ring 3's `.lodi` instances for the aggregated trees are REPLACED by one instance per cell pointing at the aggregate set (the `.lodi` needs a field or a table: reserved room per NATIVE1b's contract; state offset/law, reader refuses the old layout by name); the stock `.bto` path is untouched (aggregate is CS-exclusive: category 3).
4. **Cross-fade data**: the ring-2 per-tree cards and the ring-3 cell card must overlap in a band the runtime fades across; state the band (in cells or units) in the contract and the census.
5. **Gates**: (a) count identity: trees aggregated per cell == trees removed from ring 3's list; (b) picture gate with calibration: the cell's per-tree cards rendered at ring-3 distance vs the aggregate card at the same distance, same framing, silhouette coverage difference within a stated tolerance (`ww-silhouette-compare`), floor = an aggregate of the WRONG cell shown red, ceiling = the individual render vs itself; (c) height channel present and moving on the aggregate sheets (a taller forest reads taller); (d) `--aggregate` off byte-identical to the rung; (e) standalone gate for the new `.lodi` layout and the new `.lodm` kind.
6. Build, then the chain: the identity/standalone gates, `lodgen_native.sh`, `lodgen_card*`, `lodgen_octahedral.sh`, `lodgen_terrain.sh`, `ui_align.sh`, `water_ui.sh` at their baselines; every moved count explained by name.
7. Pictures: `agg_cell_sheet.png` (one cell's aggregate sheet, `ww-texel-picture`), `cmp_individual_vs_aggregate.png` (the calibrated pair from gate (b) + the diff), a render-hook far view of the region with per-tree cards vs aggregate cards, labelled. Described before cited.
8. Documents: `scratchpad/cards_agg_20260911/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md`; report `scratchpad/lane_cards_agg_report.md`; contract sections in `docs/LODGEN_CARD_SHEETS.md`, `docs/LODGEN_LODM_FORMAT.md`, `docs/LODGEN_NATIVE_LODO_LODI.md`.

## Gates
- A1 census printed before code (timestamps).
- A2 count identity per cell.
- A3 calibrated picture gate with floor and ceiling.
- A4 height moves; identity law stated and gated.
- A5 `--aggregate` off byte-identical.
- A6 standalone gate for the new layout/kind.
- A7 exe newer than every changed file; drivers rebuilt; rung == launch bytes.
- A8 no NifSkope left running; game down at every launch.

## Rules
- One build (+ counted relinks). Trees only. CS-exclusive; the stock path is never touched. If the aggregate cannot be made to match the individual render within tolerance, refuse with the numbers and the pictures -- that is a valid deliverable. Never his installed files. Never `git stash`, never commit. Plain language.

## Report
`scratchpad/lane_cards_agg_report.md`, incremental (PENDING.md first past half context): `## 0. Pre-registered gates`, `## 1. Census`, `## 2. The aggregate bake and the instance table`, `## 3. Build and gates`, `## 4. Pictures`, `## 5. Owed / red / bungo's calls`, `## 6. Mistakes`, `## 7. Finished-work skill review`.
