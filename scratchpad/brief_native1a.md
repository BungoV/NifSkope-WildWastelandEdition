# Lane NATIVE1a -- the FO4CS-native object files become REAL: hook-up, first worldspace bake, and the per-instance data bungo ruled tonight

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`, working tree. Nothing is committed (CONSTITUTION 8). Exe at launch: `release/NifSkope.exe` 2026-09-11 07:58:59, 20,927,488 B (SKEL2). Rollback rung, taken ONCE before your first link: `release/NifSkope.before_native1a.exe` (copy of the 07:58:59 bytes; never overwrite it).
- Read first, in order: `CONSTITUTION.md`; `HANDOFF.md` top block -- the RESUME paragraph, the SKEL2 block (newest exe state), then EVERY "RULING bungo 2026-09-11" paragraph from "LANE NATIVE1" onward (performance items 1-5, the gap review, the far-shadow constraint, the identity gate, the corpus hash, the FO4CS-side rulings with their data implications, the screen-size fade spec); `docs/LODGEN_NATIVE_LODO_LODI.md` IN FULL (the contract as built, sections 3-4 byte tables, 4.4 screen-size selection, 10 deviations); `scratchpad/lane_native0_report.md` in full (what NATIVE0/0b built, the pending hook-up, the gates run and not run, section 5); `scratchpad/native0_20260910/HOOKUP_CHANGE_NEEDED.md` (the patch, NOT applied, written against `src/lodgen.cpp` sha 69de2e97.. and `src/nifcli.cpp` af2a729c.. -- BOTH files have since changed; every anchor is re-counted, never trusted); `docs/LODGEN_VERTEX_PACKING.md` and `docs/LODGEN_MANIFEST_FORMAT.md` (what the stock path carries per vertex/placement: identity, AO, sway); `docs/MISTAKES.md` sections on lodgen; `MISTAKES.md` root from 2026-09-10 on.
- Skills you MUST invoke (repo tree `.claude/skills/`, identical to the live tree at launch): `nifskope-ww-lodgen` (the CLI, its byte-identity gates, the editing traps), `ww-standalone-writer-gate` (the writer/reader pair gated standalone: known-answer fixture, independent decoder, two-write identity, mutations refused by name -- run it AGAIN on the changed format before the exe gate), `nifskope-ww-resume-pending` (sections 2, 3, 4: apply the owed hook-up with a refusing script at re-counted anchors; qmake BEFORE make because `NifSkope.pro` gained sources; read the dependency back per object), `ww-anchored-hookup` (every edit to `src/lodgen.cpp`, `src/nifcli.cpp`, `NifSkope.pro` through a refusing script with `--check`), `ww-contract-provenance` (rewrite `docs/LODGEN_NATIVE_LODO_LODI.md` with every claim traced to the writer line, hash first, numbers last), `ww-spec-gate-audit` (any number NATIVE0 pre-registered that you are asked to reproduce), `nifskope-ww-build-verify` (the chain; rename-aside before the link; header-staleness sweep; every gate DRIVER binary rebuilt and `-nt`-checked, not only the exe), `ww-lodl-offline-census` if it helps for the region census, `fo4cs-census-field` for every new census word (WRITTEN and MOVES).
- Build rules: ONE build (+ counted gate-only relinks). `scratchpad/native1a_20260911/BUILDING` up before it, `DONE` after the gates. `tasklist | grep -i -E "Fallout4|NifSkope"; echo rc=$?` immediately before the link; Fallout4 up = BUILD PENDING with `PENDING.md`. bungo's own window (no `--port`) renamed aside, never killed. No other lane is alive at launch (`ls scratchpad/*/BUILDING` empty; say so). Bakes: SMALL REGIONS ONLY (standing OK: the 9-chunk Sanctuary region or comparable), headless, into `scratchpad/native1a_20260911/out/`, never his installed `Data\Terrain`, never the full Commonwealth.

## bungo's words that rule this lane (2026-09-11, verbatim, from HANDOFF.md)
- "anything we can do to improve it, performance wise ... the earlier the better" -> "1, 2" and "3 sounds good" and "5 sounds good": per-cluster screen error, cluster bounds + normal cones, per-instance bound radius, instances pre-sorted by mesh then material.
- "2 sounds good, 3 sounds good" (second round): precomputed occluders per cell; GPU cache order on every mesh.
- "1 sounds good" (aggregate ring-3 impostors) -- NOT this lane; reserve nothing, it gets its own lane after the format is real.
- "remember, we'll have far shadows cast by those lods, so a far shadow cast by a LOD tower behind me, will cover the area I'm at" and "Our shadow casters are based on the color id right now I think, so they can only occlude other objects and terrain, never themselves" -> every object is a shadow caster; identity survives into every native output.
- "Add it" (corpus hash / staleness).
- "Screen size fade to lower LODs would be nice to have within that 5x5 grid ... 3 sounds ogod" -> the cluster ladder must run from FULL detail down and each instance must be joinable to its placed REFR.

## The work -- lane 1a is "make it real, gate it, carry tonight's per-instance data"; the cluster HIERARCHY (screen error from full detail, bounds, cones, occluders) is lane NATIVE1b on the same files afterwards. Do not start 1b's work; do leave the format room for it (reserved header words named in the contract).
1. **Apply the owed hook-up** (HOOKUP_CHANGE_NEEDED.md) with a refusing script at anchors re-counted against today's `src/lodgen.cpp` and `src/nifcli.cpp`; `--check` output in the report; both files LF-only, CR asserted 0 before/after. `qmake` before `make`. Prove the three new sources are in `Makefile.Release` and that their objects get built.
2. **Re-run the standalone writer gate** on the format as it stands (`ww-standalone-writer-gate`): the synthetic pair, the independent decoder `tests/spells/lodgen_native_decode.py`, two-write identity, the mutation set. Record the counts BEFORE any format change (baseline), then again AFTER (item 4).
3. **First real worldspace pair.** `lodgen <esm> --worldspace 3C --terrain-region <Sanctuary 9-chunk> --native <dir> ...` (find the exact switches in nifcli/the report): a real `.lodo` + `.lodi`. Gates: the decoder passes on it; the instance count equals the stock path's manifest placement count for the same region (both printed, explained if they differ -- the "dropped for a base outside the table" census word is the discriminator); every `.lodi` chunk's `(cell, ref, part)` order rule holds (the fixture never exercised it -- this bake does); `--native-verify` rc=0; sizes printed against the spec's planned 7.0 / 5.1 MiB (scaled to the region).
4. **Format additions ruled tonight (v1 -> v2 of the contract; every field under `fo4cs-census-field`: WRITTEN and MOVES, with a test):**
   a. **Corpus hash** in both headers: a u64 over the object records + load order that produced the file (mirror `.lodl`'s `vhgtCorpusHash` law: state exactly what is hashed, in what order); `--native-verify` recomputes and refuses on mismatch, naming the file; a reader can read it without parsing anything else (header field, offset stated).
   b. **Placed REFR formID per instance record** (u32, load-order-remapped as `mapFormID` does everywhere else) so a runtime can join an instance to the engine's object. If the 24-byte record has no room, the record grows to 32 with the change stated in the contract and the reader refusing the old stride by name.
   c. **Per-instance bound radius**: state whether `base.boundRadius x instance scale` already satisfies the ruling (the runtime does one multiply) or a stored f32 is needed; if the multiply suffices, say so in the contract as the rule and add the gate that `scale` is present and non-zero on every record; if not, store it.
   d. **Instances pre-sorted by mesh then material inside each chunk** -- reconcile with the existing `(cell, ref, part)` order rule: the contract gets ONE sort law (mesh, then material, then cell/ref/part as tie-break, or the reverse with the reason), both readers check it, the fixture exercises it.
   e. **GPU cache order on every library mesh**: triangle order for the post-transform cache and vertex order for fetch locality, applied at emit (meshoptimizer is vendored -- name the calls); gate = the average cache-miss ratio (meshopt's own analysis) before vs after on the region's meshes, printed, with the floor that an unoptimised mesh reads worse.
   f. **Identity survives**: every instance record carries the identity index the stock path puts in R+G; gate = identity present and UNIQUE per placement across the region's `.lodi`, and equal to the manifest's index for the same placement (the stock path is the master).
   g. **Shadow-caster constraint on the library meshes**: no decimated/emitted mesh opens a silhouette: gate = for each mesh, boundary-edge count of the emitted mesh <= boundary-edge count of its ring-0 source (+ a stated tolerance), printed per mesh, floor = a deliberately hole-punched mesh shown red.
5. **Contract rewrite** (`ww-contract-provenance`): `docs/LODGEN_NATIVE_LODO_LODI.md` becomes the v2 contract as built: header/record tables with offsets, the sort law, the hash law, the reserved room for 1b (cluster error, bounds, cones, occluder table), every refusal by name, provenance footer. The STATUS line stops saying "not yet built into the exe".
6. **Region bake through the GUI path too**: the LOD panel's FO4CS target with the native output on (if the panel has no row yet, the CLI is the gate and the report says the panel row is LODUI1's) -- the four stage times printed.
7. Build, then the chain: the standalone gate (item 2, after), `lodgen_native_decode.py` on the real pair, `--native-verify`, `tests/spells/lodgen_*.sh` that the change reaches (name them; `lodgen_terrain.sh` 14/14 and the object-chunk suites at their baselines -- the STOCK path must be byte-identical with `--native` off: run the region bake without `--native` on the rung exe and on the new exe and `cmp` every output file), `water_ui.sh` (86/0), `animws.sh` (72/0, 1 skip), `ui_align.sh` (11/0). Every count that moves is explained by name.
8. Pictures through the render hook: the region's stock `.bto` chunk beside the same region reconstructed from `.lodo`+`.lodi` by the decoder into a NIF (or a viewer path if one exists), same camera, labelled; described before cited. If no reconstruction path exists, say so and give the numeric coverage instead (mutual nearest-neighbour of instance positions vs stock placements, as docs/LODGEN_PARITY.md does).
9. Documents: `scratchpad/native1a_20260911/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md`; report `scratchpad/lane_native1a_report.md`. You do NOT edit `WW_CHANGES.md`, `HANDOFF.md`, `MISTAKES.md`.

## Gates (this lane)
- N1 hook-up applied at re-counted anchors; `Makefile.Release` names the three sources; objects built.
- N2 standalone gate green before and after the format change (counts printed both times; the mutation set gains one mutation per new field, each refused by name).
- N3 real region pair decodes; instance count vs manifest placements reconciled; order rule exercised; `--native-verify` rc=0.
- N4 every new field WRITTEN and MOVES (a test per field per `fo4cs-census-field`).
- N5 stock path byte-identical with `--native` off (cmp over every output file, rung vs new exe).
- N6 identity unique per placement and equal to the manifest's.
- N7 silhouette gate per mesh with its floor shown red.
- N8 exe newer than every changed file; every gate DRIVER rebuilt and `-nt`-checked; rung == the 07:58:59 bytes.
- N9 no NifSkope left running; game down at every launch.

## Rules
- One build (+ counted relinks). No cluster hierarchy, no occluders, no aggregate cards -- 1b and later. No panel rows (LODUI1). No terrain changes (TERRAIN-R).
- Small regions only. Never his installed files. Never `git stash`, never commit. Plain language.

## Report
`scratchpad/lane_native1a_report.md`, incremental (PENDING.md first if past half your context):
- `## 0. Pre-registered gates` (N1-N7 with the numbers predicted; the baselines measured on the rung)
- `## 1. The hook-up` (anchors re-counted, `--check` output, qmake dependency read-back)
- `## 2. Standalone gate, before and after`
- `## 3. The first real pair` (counts, sizes, order rule, verify)
- `## 4. The v2 fields` (one subsection per field a-g: law, offset, test that it moves)
- `## 5. Build and gates` (mtime table incl. the rung and every driver; gate table with baselines and log paths; stock-path cmp result)
- `## 6. Pictures or coverage numbers`
- `## 7. Owed / red / bungo's calls` (and what 1b inherits, with the reserved room named)
- `## 8. Mistakes`
- `## 9. Finished-work skill review` (loaded, missing, written/amended -- name amended skill files for mirroring)
