# Lane RESUME3 -- one build for two BUILD PENDING lanes: NIFPARSE1 (the loader thread-safety experiment, then its fixes) and SPLAT1 phase B (the land-texture tiling constant)

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`. Nothing is committed. You are the ONLY build lane once `scratchpad/cards_agg_20260911/DONE` exists and no `scratchpad/*/BUILDING` exists -- CARDS-AGG is alive and relinking as this brief is written (its exe at 16:17); WAIT for its DONE (poll every 60 s, at most 90 polls; if it never comes, end BUILD PENDING with `PENDING.md`). PIC-GRASS is a read-only picture lane that also waits for that DONE and then runs two headless bakes -- it may run before you; check `tasklist` before every exe launch (one NifSkope instance ever) and, if a `-no-gui` bake with `pic_grass` in its command line is up, wait for it. Exe at launch: whatever CARDS-AGG's DONE line names; rung ONCE as `release/NifSkope.before_resume3.exe`.
- Read first, in order: `CONSTITUTION.md`; `.claude/skills/nifskope-ww-resume-pending/SKILL.md` IN FULL (this lane IS that skill: sections 1-11, especially 2 -- apply the owed cross-lane change with a script at equal byte length, 3 -- qmake before make and read the dependency back, 4 -- exe newer than EVERY changed file, 5 -- one sequential chain, 6 -- when a gate fails measure and STOP, 7 -- the four places that must stop saying "not built", 9 -- a PENDING's numbers are predictions, 11 -- env-var leak in the chain); `HANDOFF.md` top block: the CLOCK CORRECTION note, the NIFPARSE1 block, the SPLAT1 block, the CARDS-AGG block (when spliced), the BAKEPERF1 block; `scratchpad/nifparse1_20260911/PENDING.md` and `scratchpad/lane_nifparse1_report.md` (the shared archive index, the worker-thread message boxes, the experiment mode, the two refusing scripts `hookup.py` and `fixes.py`, `relink_sym.sh`, `gdb3.sh`, `loop20.ps1`); `scratchpad/splat1_20260911/PENDING.md`, `CONTRACT_AMENDMENT.md`, `anchors.txt` and `scratchpad/lane_splat1_report.md` (sections 2 and 4: the 14 `TILE` sites, the engine value 341.3333 = 128/0.375 from `fLandTextureTilingMult` 1.5, the gates S1-S7); `scratchpad/lane_bakeperf1_report.md` sections 1 and 4 (the shared-state inventory and the crash bisect); `.claude/skills/nifskope-ww-crash-diagnose/SKILL.md`, `ww-parallelise-a-stage`, `ww-anchored-hookup`, `nifskope-ww-build-verify`, `nifskope-ww-lodgen`, `ww-contract-provenance`, `fo4cs-census-field`, `ww-sheet-diff`, `fo4-engine-constant-from-ini-setting` (read what SPLAT1 measured, do not re-derive).
- Build rules: qmake BEFORE make (NIFPARSE1's hook-up adds a source; CARDS-AGG added two); markers `scratchpad/resume3_20260911/BUILDING` / `DONE`; `tasklist | grep -i -E "Fallout4|NifSkope"; echo rc=$?` immediately before every link; bungo's own window (no `--port`) renamed aside, never killed; every time you write into a ledger, report, DONE line or block, the time comes from `date +%H:%M` in that step.

## The work, in this order (each step's gate before the next)
1. **Re-check both lanes' refusing scripts** against today's files (`--check`; CARDS-AGG edited `src/lodgen.cpp` after both were written -- SPLAT1's anchors moved ~137 lines once already; re-count, never trust). Print the `--check` output of NIFPARSE1's `hookup.py` and `fixes.py` and SPLAT1's anchor pass. Any anchor that no longer matches exactly once: re-anchor from the file's real bytes (NIFPARSE1's own amendment to `ww-anchored-hookup`: never retype the anchor line, read it out and print its raw bytes).
2. **NIFPARSE1 step 1: name the fault.** Apply ONLY the experiment-mode hook-up (the new mode that reads meshes up front, then builds/loads/walks/destroys models on many threads with no plugin reader, no texture cache, no archive lookup, no file reads inside the threaded part, every worker reproducing the single-thread fingerprint). qmake, build (this is the ONE build; the later steps are counted relinks), run the experiment: floors first (the flipped byte goes red), then the threaded run x20. Also take the symbolised stack of the ORIGINAL fault (`relink_sym.sh` + `gdb3.sh` on the 25-chunk region with `--chunk-threads 16`, three runs; do the stacks agree?). Report section 1 = the verdict: the loader IS or IS NOT the fault, with the experiment's numbers and the three stacks.
3. **NIFPARSE1 step 2: the fixes, only those the verdict names.** From `fixes.py`'s 25 edits, apply the subset the verdict supports (the shared archive index read/write lock and the one-time lazy-table guard are the front runners; the worker-thread message-box guard is a correctness fix regardless -- apply it; the four per-thread regex objects only if the experiment implicates them; the editor name-table pointer bug -- apply it, it is a plain bug, gate = the editor harnesses). Counted relink. Then THE GATE: `--chunk-threads 16` on both regions (9-chunk Sanctuary, 25-chunk Boston) 20 consecutive runs each, exit 0, no fault (`loop20.ps1`); byte identity of every output file vs the serial run on both regions (BAKEPERF1's comparator, refuter shown red first); stage-time table serial vs 16 threads, medians of 3; peak working set; the default for `--chunk-threads` chosen by the memory rule (state it) -- if the fan-out is still slower or still faults, the default stays 1 and the report says so with the numbers.
4. **SPLAT1 phase B: the tiling constant.** One switch `--land-tiling <units>` (and the panel? NO -- CLI + INI only, LODUI1 owns rows; say so), default **341.333** (state it as 128/0.375 in the code comment with the exe address SPLAT1 found), `--land-tiling 2048` = the exact way back; ALL 14 `TILE` sites read the one value (colour, mask, emissive, both bake paths); `_msn` must not move (it comes from VHGT). Counted relink. Gates S1-S7 from SPLAT1's PENDING: `--land-tiling 2048` byte-identical to the rung on every file of both tiles; `_msn` byte-identical at both values; the local-variance table re-run on the REAL bake at 341.333 vs SPLAT1's offline prediction (12.93 / 14.60) and vs vanilla (19.81 / 29.39); whole-tile mean |RGB| vs vanilla before/after (16.89 -> ~15.0, 21.73 -> ~20.2 predicted -- predictions, re-derive); the picture `scratchpad/resume3_20260911/images/cmp_tiling_fixed.png` (vanilla | ours 2048 | ours 341.333 | diff x4, same texels, numbers burned in, `ww-texel-picture`). Contract: apply SPLAT1's `CONTRACT_AMENDMENT.md` to `docs/LODGEN_TERRAIN_VT.md` with provenance re-stamped (`ww-contract-provenance`), including the correction that the VCLR range in section 2.5 is 203..255 not 249..255. The ring-0 runtime formula in that contract must state the tiling value too (FO4CS blends at runtime with the same repeat, or the cross-fade shows).
5. **The chain** after the last relink: `lodgen_terrain.sh` (26/0), `lodgen_terrain_vt.sh` (41/1 V9b pre-existing), `lodgen_roads.sh` (11/0), `lodgen_ground_cover.sh` (29/5), `lodgen_terrain_pbrm.sh` (14/0), `lodgen_native.sh` (18/0 or CARDS-AGG's new count), `lodgen_panel_run.sh` (125/0), `lod_generation.sh` (116/0), `lodgen_stage_times.sh` (16/0), the card/array suites, `lodl_open.sh` (23/0), `ui_align.sh` (11/0), `water_ui.sh` (86/0 with its four picture args, 82 without), the editor harnesses NIFPARSE1's brief names (`loaded_nifs`, the join/sep/dupfreeze/copypaste/vertexflags spells) at their baselines measured on the rung FIRST. Every moved count explained by name. The exe-newer sweep over the whole `git status` set; every gate DRIVER rebuilt and `-nt`-checked.
6. **The four places** (resume-pending section 7): `WW_CHANGES.md` entries for NIFPARSE1 and SPLAT1 become the measured ones -- write REPLACEMENT text to `scratchpad/resume3_20260911/WW_CHANGES_ENTRY.md` (the director splices; the two pending entries are already at the top of the file and must be superseded, say which lines); `MISTAKES_ENTRIES.md`; a `## Build (RESUME3)` section appended to BOTH lane reports; `HANDOFF_BLOCK.md`; any skill the build disproved. Append a dated paragraph to `scratchpad/lodui1_20260911/BAKE_INSTRUCTION.md` (the tiling default, the thread default, the expected time).

7. **PIC-GRASS's picture, on the FIXED tiling.** Lane PIC-GRASS timed out on its poll cap and left everything ready: `scratchpad/pic_grass_20260911/run.sh` (gate, two headless bakes of the Sanctuary loop-road chunk at `--cover` and `--cover --grass-tint 0`, then `make_picture.py` -> `images/cmp_grass_tint.png` + `images/numbers.json`, vanilla | ours default tint | ours no tint | tint footprint x4, colour error vs vanilla per panel and over cover>0 texels). Run it AFTER the tiling relink so the picture shows the tint at the engine's tiling (the bakes take the new default); then write its `NOTES.md` (the two commands, exe time/size, the numbers, two sentences per panel BEFORE any claim). Its bakes go into its own out-dir; one NifSkope at a time.

## Gates (this lane)
- R1 both scripts `--check` green at re-counted anchors before anything is applied.
- R2 the fault named from the experiment + three stacks BEFORE any fix beyond the message-box guard.
- R3 20x16-thread loops on both regions or a numbered refusal; identity vs serial; stage table; memory; the default stated by rule.
- R4 tiling: identity at 2048; `_msn` identical at both; variance and colour tables real vs predicted vs vanilla; picture.
- R5 whole chain at baseline, moved counts named; exe newer than every changed file; drivers rebuilt; rung == CARDS-AGG's exe bytes.
- R6 no NifSkope left running; game down at every launch; every timestamp from `date`.

## Rules
- One build + counted relinks. Apply fixes the verdict names, never the whole list on a guess (NIFPARSE1's own recorded lesson). No panel rows. No arithmetic change outside the tiling value. Region bakes only, own out-dir, never his installed files. Never `git stash`, never commit. Plain language.

## Report
`scratchpad/lane_resume3_report.md`, incremental (PENDING.md first past half context): `## 0. Pre-registered gates` (numbers predicted, baselines on the rung), `## 1. The fault, named`, `## 2. The fixes applied and the fan-out gate`, `## 3. The tiling fix and its gates`, `## 4. Build ledger and the chain` (mtime table incl. rung and drivers; gate table; moved counts), `## 5. Pictures`, `## 6. Owed / red / bungo's calls`, `## 7. Mistakes`, `## 8. Finished-work skill review`.
