# Lane UI6 -- strips rejoined, the old Animation Manager retired, dropdown arrows given air

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`, working tree. Nothing is committed (CONSTITUTION 8). Exe on disk at launch: `release/NifSkope.exe` 2026-09-11 05:58:21, 20,855,296 B (UI5), carrying every landed change.
- Read first: `CONSTITUTION.md`; `HANDOFF.md` top block -- the RESUME paragraph, the UI RULING paragraphs of 2026-09-10 20:1x/20:3x/21:0x/21:1x, the 2026-09-11 05:4x arrow ruling, then the UI4, WATER8 and UI5 blocks (how the row, buttons, strips and menu items are sized through the shared skin: `wwAlignBarRow`, `wwBarRowButtonQss`, `wwBarRowBoxQss`, `wwBarRowMenuItemQss`, `wwSegmentedTabBarQss`, `UI/CompactTopBars`, `UI/SegmentedStripAir`); `MISTAKES.md` from the 2026-09-10 UI4 entries to the end (UI5's five are the newest: the vanished rollback rung, the gate that measured the underline, the empty stylesheet Qt ignores); `scratchpad/lane_water8_report.md` section 4 item 2 (gate L8 measures only top/bottom; nothing measures the right strip's inter-segment gap); `scratchpad/lane_ui4_report.md` (how the strip air was built and gated); `scratchpad/lane_ui5_report.md` sections 2-4.
- Skills you MUST invoke (repo tree `.claude/skills/`, mirrored to the live tree 09:0x): `ww-qss-geometry-probe` (case 0 reproduces the shipped number in the substituted sheet; a change that moves what is PAINTED is gated on pixels, never on `tabRect()`), `ww-anchored-hookup` (every edit to a file you do not own outright goes through a refusing script with `--check`), `ww-test-harness-add` (floors that fire live; the translation-unit rule), `nifskope-ww-build-verify` (the chain; the exe under bungo's window renamed aside IMMEDIATELY before the link, inside the chain; `cmp res/style.qss release/style.qss`; the exe-newer sweep over the whole `git status` set; UI5's compile-now/link-later procedure), `nifskope-ww-panel-style` (scrub fields, no text, the self-test counts), `nifskope-ww-build-verify` "Whose NifSkope is that" before every exe launch.
- Build rules: ONE build. Put up `scratchpad/ui6_20260910/BUILDING` before it and replace with `DONE` after the gates. `tasklist | grep -i -E "Fallout4|NifSkope"; echo rc=$?` immediately before the link; Fallout4 up = end BUILD PENDING with `PENDING.md`. bungo's own window (no `--port`) is renamed aside, never killed. Keep a real rollback rung: copy the 05:58:21 exe to `release/NifSkope.before_ui6.exe` ONCE, before your first link, and never overwrite it (UI5 lost its rung by copying before every link).

## bungo's words, verbatim
- 2026-09-10 21:0x, zoomed screenshot of the Header | Blocks | Files strip after UI4 gapped its segments: **"Why are they separated?"** -> the segments of a strip TOUCH; only the strip's outer box keeps UI4's 4 px from the row top/bottom, the left edge and the toolbar.
- 2026-09-10 21:1x, three screenshots of the OLD Animation Manager (the interim HKX3 strip inside `timeline.cpp`): two spin boxes (0.050 / 0.1000) with their steppers clipped to a sliver -- **"Do you see it?"**; **"we have a new standard for those sliders, don't you remember?"** (numbers are scrub fields, never bare spin boxes); **"look at all this text clutter"** (the binding report as a paragraph in the dock). His earlier ruling: the old manager is "old and outdated"; the new Animation dock (gated 57/0 by animws.sh) is the replacement.
- 2026-09-11 05:4x, over `scratchpad/ui3_20260910/images/cmp_zoom.png`: **"That's fine, as long as the dropdown arrows do not intersect with the text / icons like on the screenshots you showed me"** -> the viewport-header buttons keep UI3's size; the menu arrow on every one of them gets air so it never overlaps the icon or the label (the pivot-dot and grid icon buttons overlap today).

## The work
1. **Strips rejoined.** Both segmented strips -- LEFT `LeftColumnModeSelector` (Header | Blocks | Files) and RIGHT `LodPanelModeSelector` (LOD | Water) -- become ONE joined box each: inter-segment gap 0 px, the segments sharing the original box (inner corners square where segments meet, outer corners as UI4 left them), outer air 4 px on all four sides unchanged, segments 27 px high in the 35 px row unchanged, `UI/SegmentedStripAir` still the way back. Probe case 0 first (`ww-qss-geometry-probe`): reproduce 4 px between segments in the substituted sheet, then the fix. Gates: UI4's S5 (left strip inter-segment gap) changes from `4..4` to `0`; L8 gains its MISSING half (right strip inter-segment gap 0) -- both read PIXELS (painted box edges), never `tabRect()`; both floors shown to fire live in the same log by running against the old sheet's numbers.
2. **Old Animation Manager retired.** Remove the old Animation Manager dock and the interim clip strip inside `src/ui/widgets/timeline.cpp` (find every entry point: Workspaces menu, View > panels, the dock's objectName, any harness that lists it -- `top_bar.sh` may quote its menu entry: repair EXPECTED LITERALS only, per resume-pending section 10, and record the pre-change baseline first). The new Animation dock stays the one animation surface; every function the old manager still offered that the new dock lacks is LISTED in the report (do not port silently; the director decides). The `timeline.*` playback plumbing the new dock depends on is not touched -- read the HKXEDIT2 block to learn what the new dock calls.
3. **Scrub fields on every number in the new Animation dock.** Every numeric field goes through `wwMakeScrubField` / `WwNumberField` (panel-style). Gate: count of numeric spin boxes in the dock NOT stamped as scrub fields == 0, with a floor (a deliberately un-stamped box in a test-only widget counted as 1 in the same run). Gate: every spin box's stepper rect is fully inside its widget rect and >= 8 px wide (none clipped), floor: the old 0.050 / 0.1000 clip reproduced against the old exe or the old sheet if it still exists, else the report says why the floor could not be shown.
4. **Binding line cut to a summary.** The bone-binding report in the new dock (if present there; the old manager's paragraph goes with the old manager) becomes a few words -- e.g. "78 of 95 bones" -- with the names in a tooltip or a fold. No paragraph, no blurb (bungo's no-text rule). Gate: the dock's visible label text for the binding is <= 40 characters and contains both numbers.
5. **Dropdown arrows given air.** On every viewport-header button with a menu (`Global`, pivot, snap, grid -- find them all in `nifskope_ui.cpp`, the UI3 block names them), the menu-indicator column must be >= 2 px right of the icon's or label's last ink column, without changing the button height (35) or the row. Through the skin (`wwBarRowButtonQss` or a sibling), not per-button literals. Gate: a pixel read on the in-app grab, per button: first arrow-ink column minus last icon/label-ink column >= 2; floor: the current exe's grab read by the same code gives < 2 on the two icon buttons (name them) and the log prints both.
6. Build once (item order above; all in one link), then the chain: `water_ui.sh` (`env SHOT=scratchpad/ui6_20260910/images/toprow_after.png`), `ui_align.sh`, `top_bar.sh`, `files_tab.sh`, `animws.sh`, `hkxanim_ui.sh`, `loaded_nifs` (its spell). Baselines: water_ui 76/0 floor 62 (UI5); ui_align 11/0; top_bar 43/5; files_tab 28/2; animws 57/0 (1 skip); hkxanim_ui 48/1; loaded_nifs 166/0. Every count that moves from baseline is explained by check NAME.
7. Pictures (in-app grabs, never desktop): before/after of each strip at 4x zoom (`cmp_strip_left.png`, `cmp_strip_right.png`), before/after of the viewport header at 4x (`cmp_arrows.png`), the new Animation dock after (`animdock_after.png`), and one grab proving the old manager is gone (the Workspaces/View menu dump in the log is acceptable for that). Before-pictures come from a SIBLING spell already in the 05:58 exe, taken before your hook-up is applied (resume-pending section 11). Describe each in two sentences before citing it.
8. Documents: `scratchpad/ui6_20260910/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md`; report at `scratchpad/lane_ui6_report.md`. You do NOT edit `WW_CHANGES.md`, `HANDOFF.md`, `MISTAKES.md`.

## Gates (this lane)
- G1 left strip: inter-segment gap 0, outer distances 4/4/4/4, segment height 27, row 35 -- pixels.
- G2 right strip: the same five numbers -- pixels (L8's missing half now exists).
- G3 no widget with the old Animation Manager's objectName exists; no Workspaces/View entry names it; `animws.sh` still 57/0.
- G4 unstamped numeric boxes in the new Animation dock == 0 with a floor of 1 shown; no stepper clipped.
- G5 binding label <= 40 chars with both numbers.
- G6 every viewport-header menu button: arrow gap >= 2 px, floor shown on the old grab.
- G7 exe newer than every file in `git status --porcelain -- src res tools tests`; `cmp res/style.qss release/style.qss` silent; `release/NifSkope.before_ui6.exe` == the 05:58:21 bytes (size 20,855,296).
- G8 no NifSkope left running by you; bungo's window untouched.

## Rules
- One build. Files another lane could own go through the refusing hook-up script. No other lane is alive at launch; say so in the report after checking `ls scratchpad/*/BUILDING`.
- Touch nothing in the LOD panel's ROWS (lodgenmanager.cpp settings) -- that is lane LODUI1's. The right strip's QSS/geometry is yours.
- Never `git stash`, never commit. Plain language in the report.

## Report
`scratchpad/lane_ui6_report.md`, incremental:
- `## 0. Pre-registered gates` (numbers predicted for G1-G6 before code)
- `## 1. Case 0 and the old manager's inventory` (probe numbers; every function the old manager has, and whether the new dock has it)
- `## 2. The changes` (per item: files, helper, hook-up `--check` output)
- `## 3. Build and gates` (mtime table incl. the rollback rung; gate table with baselines, log paths, moved counts by name; skipped harnesses with reasons)
- `## 4. Pictures`
- `## 5. Owed / red / bungo's calls` (incl. anything the old manager could do that the new dock cannot)
- `## 6. Mistakes`
- `## 7. Finished-work skill review` (loaded, missing, written/amended -- name amended skill files for mirroring)
