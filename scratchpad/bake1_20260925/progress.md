# BAKE1 progress (clock read with date +%H:%M)

- 03:43 briefs + skills read; FO4CSLOD absent; game down; worktree at 9f37fa7.
- 03:46 step 1: main objects current (make -n 0 g++); copied .qmake.stash, GeneratedFiles, release runtime; qmake rc 0 (123 worktree paths, 0 main); tools/ww_build.sh rc 0, 0 g++ (relink only). exe sha1 3eaefafa. Run copy = scratchpad/bake1_20260925/run/ (57 files, 106 MB).
- 03:5x FOUND: on the rung exe his 47-plugin load order lists 36 tree candidates, identical to vanilla, 0 from BNS Trees.esp; chunk (-4,-24) places the same objects both ways. Cause: the ESM reader merges plugins record by record; a later plugin's own world group and every cell group it opens under an OVERRIDDEN cell hang from no record, so BNS Trees.esp's 22,827 REFRs never reached a bake. Fix src/esmdata.{h,cpp} (patch_esmgroups.py): enumerate the group ids once, walk later world groups, join later cell groups (incl. persistent) to their cell. Build 2 rc 0, exe d06a5716.
- 03:57 verified: trace "12 later world groups, 1636 later cell groups joined, 12 persistent". Chunk (-4,-24) on his load order: rung 362 placements / 0 BNS, fix 463 / 92 BNS. Vanilla-only chunk byte-identical rung vs fix (sha1 d6e64432). Second fix src/nifcli.cpp (patch_candpersist.py): the candidate lister now reads the persistent overlay like the chunk builder. Build 3 rc 0, exe 256ea8b9. Tree candidates on his load order: 79 (36 vanilla, 1 DLCCoast, 42 BNS); vanilla-only list unchanged (36, same set).
- 04:01 step 2 running: tree card bake (79 candidates: 36 vanilla, 1 DLCCoast, 42 BNS; N8, RING 0, TILE 256 default, every mesh from his MO2 stack via --probe, DATAROOT pointed at nothing). ~35 s a model.
- 04:01 PANEL GAP FOUND: under the FO4CS target the "Ground cover and grass tint" row lives in the stock-terrain section, which is hidden, and coverOptions() needs the stock .BTR and texture rows ticked -- so the panel cannot give cover ON (R4) without writing a stock .BTR/sheet set into the mod folder (R2). The bake therefore runs as the panel's command-line equivalent (bake.sh: .lodl, heightmap, then VT + chunks + native + cards + arrays, --vt-density 16 = the panel default, stock set to scratch). Also: the CLI puts the .lodb beside --out-dir (scratch), not in the mod.
- 04:01 step 3 running: dry run, region -24 16 -9 31 (4x4 dim-4 chunks, Sanctuary), into dry/ (scratch), cards library partial (card bake still running; timing only).
- 04:05 heightmap stage refused on his load order (land corpus hash cd9657c5, FO4CS pins d8337d02; same file name as the FO4CS mod's own map, so a copy would shadow it). bake.sh skips it by default and says why; reported, not dropped silently.
- 04:07 dry run CRASHED (rc 139) in the chunk stage. Repro on one chunk (-20,24): fixed exe segfaults in 9 s, rung exits 0 (678 placements, 0 BNS). Stack (no symbols; functions named by their strings, fnstrings.py): lodoAppendMesh <- lodoStageMesh <- the native library pass. Cause: BNS Trees' LOD models carry vertices no triangle uses; meshopt_optimizeVertexFetchRemap returns ~0u for those and the hand-applied remap wrote pos[~0u*3]. Fix src/lodofile.cpp (patch_fetchremap.py then patch_fetchremap2.py): unused vertices take the slots after the used ones -> a true permutation. The inline form moved 2 vanilla self-AO bytes (codegen), so the repair lives in a noinline helper.
- 04:15 build 5 rc 0, exe dabeef90. Red: 24f5f39b exe on his load order, chunk (-20,24) = rc 139. Green: this exe = rc 0, 949 placements (678 vanilla + 271 BNS). Vanilla-only native bake rung vs fix: .lodo .lodi .lodj .BTR manifest byte-identical. Run copy refreshed.
- 04:13 step 2 DONE: 79 of 79 tree card sets baked in 15 min 26 s (03:57:50 -> 04:13:16), 0 empty (lowest coverage 7.3 %, 000531ae), 42 BNS; 49 sway model / 30 synthetic; 185 MB.
- 04:32 CARDS NEED THE FAR RINGS: the command line's region bake is one ring (--dim 4), and a card only stands in for a tree at ring 16/32, so the dim-4 dry run linked 0 cards ("no placement stands on an octahedral card"). The panel's "all rings (4+8+16+32)" does all four in one pass. Patch patch_dimall.py: `--dim all` = that, in the panel's queue order; the bake record keys another ring's chunk `cx,cy@dim` (a one-ring record is unchanged); --incremental with --dim all is refused, as in the panel. Build 6 rc 0.
- 04:34 ONE ROOT: the CLI roots the arrays, chunk manifests and the .lodb at --out-dir, so with the stock set in scratch they would land outside the mod (the census calls it "a SECOND root"). Opt-in `--fo4cs-one-root` (patch_oneroot.py) roots them at --native, the panel's layout; off = unchanged. Build 7 rc 0, exe 96c99832. Vanilla one-chunk bake rung vs build 7 with no new switch: .lodo .lodi .lodj .BTR manifest byte-identical; record identical except the chunk input digest, which hashes the exe's own bytes by design. One-chunk test with the switch: layout "28 file(s), 0 outside", no second root.
- 04:32 DRY RUN (step 3), his load order, full settings, region -24 16 -9 31 all rings (23 chunks: 16 x4, 4 x8, 2 x16, 1 x32): rc 0 in 492 s (meshes 298, VT 54, card arrays 128); cardCount 62 of 3064 bases, FORCE_CARD on 20,165 of 35,674 instances; 142 MB in the mod + 359 MB arrays/stock. Ring-4-only run of the same region: 140 s. WHOLE-MAP ESTIMATE: the worldspace is -96..95 (36,864 cells, 3,060 chunk jobs over 4 rings). VT: 84 tiles took ~54 s; the whole pyramid is 12,276 tiles, 16.8 GB (+2.1 GB stock BTR sheets to scratch) -> up to ~2.2 h. Chunks + cards: the dry run's 1,024-cell far-ring block cost ~440 s; the playable land is ~4 such blocks, open sea is cheap -> ~0.5-1.5 h. Total ~3-4 h, ~20-25 GB in the mod, ~5-10 GB scratch. Under the 5 h / 100 GB line: no setting changed. The early VT tile rate of the real run is the check; if it projects past 5 h it is stopped and re-planned.
- 04:39 THE BAKE launched 04:37:17 (bake.sh, whole map -96 -96 95 95, --dim all, run copy exe 96c99832, into mods\FO4CSLOD -- absent at 04:36, re-checked right before). G5 before-listings g5_*before2.txt taken 04:36. lodl stage rc 0 in 5 s (36.0 MB). VT pyramid writing: 1.03 GB in ~85 s at 04:38:48.
- 04:46 bake running, VT stage: 5.57 GB written by 04:46:42 (~10 MB/s; pyramid 16.8 GB -> VT done ~05:05, well inside 5 h). G4 instrument ready (g4_cover.py, mmap over Lodv): grass cells picked INDEPENDENTLY from the plugins (grass_cells.py: LAND quadrant LTEX with GNAM; 115 of 256 in the dry region). Dry-run VT (cover ON): cells -20,25 / -24,24 / -22,22 non-uniform (153/135/174 distinct values); control cells with no grass texture read uniform 93. RED g4red/ (same region, no --cover): mask BC1, no cover channel on all 3 -> RED as designed. G3 instrument g3_cards.py: dry2 32 BNS (2c) bases carry a card; RED vanilla-only bake 0 (no 2c base at all), RED dim-4 run on his order 0 of 42 BNS bases. G2 red: vanilla record lists 1 plugin. Skills written: ww-stripped-frame-by-strings, ww-meshopt-unused-vertices, ww-esm-later-plugin-groups.
- 05:02 bake running, VT stage: 10.34 GB of the 16.8 GB pyramid by 05:01:52 (5.2 MB/s over the last 9.5 min) -> VT done ~05:23, then chunks + cards. Picture recipe proven on the dry-run output (shot.sh: .lodl + VT sheets + .lodi objects, top view, 947 placements drawn, 0 missing mesh). Places for the chunk views read from Fallout4.esm cell names (cell_names.py): Sanctuary -21..-18,20..22, Concord -15,17, Diamond City -4,-8, Boston Common 2,-5; BNS-densest chunk -4,-24 (bns_cells.py); grass-densest 8x8 block -24,-8 (grass_cells.py, 63 grass cells).
- 05:41 bake running. VT stage finished ~05:07: 12,276 of 12,276 tiles present, 15.25 GB, 1,522 cover tiles, cover 1 (the log's own vt line). Terrain queue ([1]..[3546], .BTR scaffolding + stock sheets to scratch, 5.25 GB there at 05:23) finished 05:40. Now "chunk pass: 3060 job(s) over 1 worker(s)" (the objects). Mod folder 15.4 GB at 05:23.
- 05:53 THE BAKE DIED at 05:47:14 after 4192 s (chunks stage rc 127; the VT, the terrain queue, the 3060-chunk pass, the card arrays (79 sets) and the .lodo were done; no .lodi, no .lodb). Log: "Qt Concurrent has caught an exception thrown from a worker thread" three times, then "terminate called recursively". Cause, read in the code: the v6 vertex-AO loop (src/nativeemit.cpp, one scene a chunk, chunks on the thread pool) reads LAND through the ONE shared EsmWorld, and libfo76utils' ESMFile::uncompressRecord rewrites the reader on a record's first decompression (zlibBuf.back() resized, a buffer emplaced, the record's flags/data pointer rewritten) -- two workers on two never-read LAND records corrupt each other and errorMessage() throws from a pool thread. The dry run could not see it: 23 chunks is under lodgenParallelFor's 32-item floor, so its loop ran SERIALLY. Memory ruled out first: ao_field_estimate.py over the bake's own .lodj + .lodo puts the largest scene (dim 32, field -32,0) at ~5.0 M triangles, ~0.5 GB. Fix patch_landlock.py: the land() read under a QMutex, heights copy + rays still parallel; build 8 rc 0. Gate running: 36-chunk ring-4 native bake (above the floor, so the pool path) with build 7 vs build 8, .lodo/.lodi must be byte-identical.
- 06:02 LAND-LOCK GATE (landlock_gate.sh, 36 ring-4 chunks -24 8 -1 31 on his load order, so the vertex-AO loop takes the pool path): RED build 7 = rc 127, "Qt Concurrent has caught an exception thrown from a worker thread", no .lodi (the crash reproduced in 140 s). GREEN build 8 = rc 0, .lodi 98de95de, .lodo c57fbe65. BYTE REFERENCE build 7 with --threads 1 (the serial way back) = rc 0, .lodi 98de95de, .lodo c57fbe65: build 8 on the pool is byte-identical to the old code run serially. Run copy exe = build 8 (27a7bb29). The whole bake is re-run from a cleared mods\FO4CSLOD (my own output only).
- 06:16 run 2 (build 8) running: lodl rc 0 06:01:57, VT stage writing, 7.54 GB so far. Meanwhile: verify.sh (step 6 readers) and g5_after.sh (G5 listings + diff) written; G5 mid-run preview shows only ./FO4CSLOD new and ./mods' own mtime moved. Skill ww-shared-reader-in-pool written.
- 06:46 run 2: VT done, terrain queue at [3418] (ring 16), 0 Qt Concurrent lines. Run 1 reached the same point ~05:23 from a 04:37 start; run 2 is on the same pace (~46 min in).
- 07:01 run 2: terrain queue done, object chunk pass (3060 jobs) running; run 1 crashed ~7 min into the end of this pass, so the next 15 min is the test of the fix at scale.

- 07:38 (clock read) run 2 DONE rc 0: chunks 5728 s (06:01:57-07:37:25). The 07:07-07:37 single-core phase was the proximity join (98,849 eligible placements, 12.16 M samples, 1,722,649 ms serial) inside the instances stage (1742.6 s). Slow, not stuck. .lodb: 47 plugins, 3676 files, 15.96 GB. Peak working set 9.23 GB. Starting verify.sh + G2-G5.
- 07:43 (clock read) VERIFY: --native-verify rc 0 (cardCount 79); census check 38 checks 0 failures, floor ok (2 doctored claims caught); lodm/lodl/lodb rc 0; VT.32..4 lodt-check rc 0. G2: .lodb 47 plugins = 30 active in plugins.txt + 8 base ESM + 9 CC ESL, incl. BNS Trees/Landscape, Grasslands - Healthy, TrueGrass. G3: 79 card bases, 42 from BNS Trees (2c). G4: cells -20,25/-24,24/-22,22 non-uniform (153/135/174 distinct), PASS. G5: only ./FO4CSLOD new + ./mods mtime; profile 13/13 unchanged. FINDING: FORCE_CARD 0 of 184,431 (dry run 20,165): whole-map ring 4 now reaches every tree first, the record keeps that arrival (slot 0, a mesh), so the far-ring C lines never key it; slotInstances full 184069/342/20/0 vs dry 12756/1/8364/14553.
- 07:57 (clock read) PICTURES: 8 labelled views + overview + contact sheet in pics/ (not committed), all checked by eye and numbers (lum SD 17-42). DONE.md, DELIVERABLE_TEXT.md, pics/INDEX.md written. Skill ww-whole-map-lod-bake written.
