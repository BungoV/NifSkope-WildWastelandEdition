# Lane TILING2 -- the far-terrain colour after the tiling fix: the texture repeat must not show, the blend edges must be as soft as vanilla's, and the sheet must not be blurrier than vanilla's

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`. Nothing is committed. Exe at launch: `release/NifSkope.exe` 2026-09-11 20:46:44, 21,458,944 B (ROADS2's DONE exe; ROADS2 changed the road composite -- read its HANDOFF block and `scratchpad/lane_roads2_report.md` too, its roads suite is 11/0 and must stay so). Rung ONCE: `release/NifSkope.before_tiling2.exe`. Markers `scratchpad/tiling2_20260911/BUILDING` / `DONE`. Every timestamp from `date +%H:%M` in the same step. One NifSkope instance ever; game check before the link; bungo's window renamed aside. Region bakes only, own out-dir. Never his installed files.
- Read first: `CONSTITUTION.md`; `HANDOFF.md` top block -- CLOCK CORRECTION, the RULING 19:2x paragraph (this lane's words), the RESUME3 block (gate R4: the tiling switch, `--land-tiling`, default 341.3333, its measurement table and the red on chunk (-20,20)), the SPLAT1 block, the TERRAIN-R block; `scratchpad/resume3_20260911/make_tiling_picture.py` + `images/cmp_tiling_fixed.png` + `logs/tiling_measure.txt` (the picture bungo judged; its numbers); `scratchpad/splat1_20260911/splatlib.py` (DDS reading, local variance, the phase-randomised twin); `scratchpad/lane_splat1_report.md`; `docs/LODGEN_TERRAIN_VT.md` (the colour law); `src/lodgen.cpp` `lodgenBakeTerrainTextures`: the `sampleLtex` lambda (hand wrap at `TILE`, mip = log2(footprint / texelWorld) -- one bake texel reads ONE mip texel of the repeat, which is the pattern), the per-quadrant `baseTex[q]` / `dominantBase` selection, the per-layer bilinear over the 17x17 quadrant opacities, the "negligible layer" skip, the VCLR multiply; `MISTAKES.md` root from 2026-09-11.
- Skills: `nifskope-ww-vanilla-compare`, `ww-control-calibration`, `ww-sheet-diff`, `ww-texel-picture`, `ww-spec-gate-audit`, `nifskope-ww-lodgen`, `ww-anchored-hookup`, `nifskope-ww-build-verify`, `ww-contract-provenance`, `ww-module-off-is-identical`.

## bungo's words (verbatim, 2026-09-11 19:2x, over the "OURS default 341.3333" panel of cmp_tiling_fixed.png)
"Asking, because there's a few issues here" ... "Yes, you can see the tiling pattern of each texture, which is not good, hard blend edges also appear in some places, and yeah, it's muddy or blurry looking"

## What is known going in
- The sheet in the picture is dim 4, 512 texels per chunk, 32 world units per texel; one land-texture repeat = 341.3333 u = 10.67 texels. `sampleLtex` picks the mip whose texel equals the bake texel's footprint, so each repeat is rendered as the same ~11-pixel picture of the texture and the repeat itself becomes a regular pattern. Vanilla's sheet shows no such pattern.
- Local variance after the fix: chunk (-20,24) ours 12.34 vs vanilla 19.81; chunk (-20,20) ours 25.83 vs vanilla 29.39 and 77 % off SPLAT1's prediction (RESUME3's red; explain it or inherit it with the number).
- Layer opacities are bilinear over 17x17 per quadrant (128 u spacing = 4 texels), so a painted transition is already soft; the hard edges therefore come from somewhere else -- candidates: the quadrant border (base texture and layer SET change per quadrant, nothing blends across the 64-texel quadrant grid), the negligible-layer skip, a base-texture switch, the cover/road planes. Measure where they are before deciding.
- Tone is GRADE1's (queued after this lane): do not touch gain/gamma/saturation. The tiling constant is RESUME3's: do not change it.

## The work
1. **Measure vanilla's law on the three complaints, before code**, on both tiles and on 20 more shipped dim-4 sheets picked across the Commonwealth (name them):
   a. **Periodicity**: 2-D autocorrelation of the colour sheet; the peak at lag 10.67 texels (both axes, and the diagonal) relative to lag 0, for vanilla, for ours at 341.3333, for ours at 2048 (lag 64), and for the phase-randomised twin (floor). State it as one number per sheet, "tiling visibility". Vanilla's value is the target; the twin's is the floor.
   b. **Edge width**: on the strongest blend edges (top decile of gradient magnitude) the 10-90 % transition width in texels, histogram, vanilla vs ours; AND the position of our hard edges against the quadrant grid (every 64 texels at dim 4) and against the road/cover planes. Name the mechanism with the count ("N of M hard edges lie on a quadrant border").
   c. **Detail**: the radial power spectrum of vanilla's colour vs ours (both tiling values): at which spatial frequencies does vanilla carry energy ours lacks, and how much. Then the candidates for where vanilla's detail comes from, each tested by correlation of vanilla's high-pass residual against: the blended land textures at each mip (footprint mip through the fully averaged one), the VCLR, the height/slope/AO planes, and the twin floor. Report the table; the winner is the one above the floor with the right spectrum. If none correlates, say so -- then vanilla's detail is a noise or lighting term the bake has no source for, and the lane REFUSES that part with the numbers (a refused round is a deliverable).
2. **The repeat**: bake the land-texture contribution so the repeat cannot show. The candidate is the repeat-averaged colour: integrate the texture over the whole repeat (the top mip, or an explicit box average over one period) instead of the footprint mip, with a detail term added back ONLY if 1c found its source. Behind `--land-sample footprint|average` with `footprint` = the rung's bytes. Gate: tiling visibility (1a) on both tiles at or below vanilla's, and above the twin floor only by what the detail term explains.
3. **The edges**: whatever 1b named. If it is the quadrant border, blend across it: sample the neighbouring quadrants' composites within a margin of the border and lerp, or evaluate the neighbour quadrant's layer set with its own opacities (which reach zero at the border only when the artist painted them so -- measure how often they do). If it is the negligible-layer skip, lower or remove the threshold and show the cost. Behind `--blend-edges off|<mode>`, off = rung bytes. Gate: our edge-width histogram inside vanilla's (same decile), no new seam at chunk borders (cmp the shared border row of the two tiles).
4. **The blur**: from 1c's winner only. If the detail source is the land textures at a finer mip, the term is the difference between the footprint sample and the average, scaled by a fitted strength, added to the average (this is 2's detail term, not a third switch); if it is VCLR or a lighting plane, name it and route to GRADE1 (do not bake a lighting term here). Gate: local variance and radial spectrum of ours within 20 % of vanilla's on both tiles, floor = the rung, ceiling = vanilla vs itself across neighbouring shipped tiles.
5. Build (one build + counted relinks), then the chain at the baselines from RESUME3's block; both switches at their off values == rung bytes (cmp every file of both tiles); `lodl_open.sh` / `lodgen_terrain.sh` / `lodgen_terrain_vt.sh` / `lodgen_roads.sh` / `lodgen_ground_cover.sh` / `lodgen_terrain_pbrm.sh`; the pyramid contract's tests; the `.lodl` weights and `_msn` byte-identical at every setting (they carry no colour).
6. Pictures, same crop and layout as `cmp_tiling_fixed.png` so bungo can put them side by side: `cmp_tiling2.png` = vanilla | ours footprint (the panel he judged) | ours average+edges+detail, numbers burned in (tiling visibility, edge width p50/p90, local variance, spectrum distance); `cmp_edges.png` = the strongest hard edge before/after with vanilla's same texels; `cmp_detail_spectrum.png` = the three radial spectra on one plot.
7. Documents: `scratchpad/tiling2_20260911/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md` (entries start with `## `); report `scratchpad/lane_tiling2_report.md`; `docs/LODGEN_TERRAIN_VT.md` colour-law amendments with provenance; `scratchpad/lodui1_20260911/BAKE_INSTRUCTION.md` updated if a default changed (say the default; it should be the vanilla-matching one).

## Gates
- F1 the three laws measured on vanilla over 22 sheets before code, each with a floor and a ceiling.
- F2 switches off == rung bytes; `.lodl` and `_msn` untouched at every setting.
- F3 tiling visibility at or below vanilla's on both tiles; edge widths inside vanilla's histogram; no chunk-border seam.
- F4 detail: within 20 % of vanilla's local variance and spectrum, or refused with the correlation table.
- F5 chain at baseline; exe newer than every changed file; drivers rebuilt; rung == launch bytes; no NifSkope left running.

## Rules
- One build (+ counted relinks). No tone changes (GRADE1's). No tiling-constant change (RESUME3's). Never his installed files. Never `git stash`, never commit. Plain language. Every number in the report has its floor beside it.

## Report
`scratchpad/lane_tiling2_report.md`, incremental (PENDING.md first past half context): `## 0. Pre-registered gates`, `## 1. Vanilla's laws (periodicity, edges, detail)`, `## 2. The repeat`, `## 3. The edges`, `## 4. The detail term`, `## 5. Build and gates`, `## 6. Pictures`, `## 7. Owed / red / bungo's calls`, `## 8. Mistakes`, `## 9. Finished-work skill review`.
