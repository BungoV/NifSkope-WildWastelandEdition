# Lane NIFPARSE1 -- the NIF model loader made thread-safe, so the bake's chunk fan-out can ship ON

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`. Nothing is committed. Exe at launch: `release/NifSkope.exe` 2026-09-11 14:48:52, 21,261,312 B (BAKEPERF1) -- but lane CARDS-AGG is ALIVE and holds the build slot and `src/lodgen.cpp`, `src/nativeemit.*`, `src/lodofile.*`, `src/lodifile.*`, the card/impostor code and `NifSkope.pro`. You are CODE-ONLY until `scratchpad/cards_agg_20260911/DONE` exists and no `scratchpad/*/BUILDING` exists (poll every 60 s, at most 90 polls; if it never comes, end BUILD PENDING with `PENDING.md` per `nifskope-ww-resume-pending`). When you build: rung ONCE `release/NifSkope.before_nifparse1.exe` from whatever exe CARDS-AGG left (read its DONE line), markers `scratchpad/nifparse1_20260911/BUILDING` / `DONE`, game check immediately before the link, bungo's window renamed aside, never killed.
- Read first: `CONSTITUTION.md`; `HANDOFF.md` top block -- RESUME, the BAKEPERF1 block and the "FROM BAKEPERF1" owed paragraph (bungo 15:5x "Yeah, go for it" = this lane); `scratchpad/lane_bakeperf1_report.md` IN FULL, especially the shared-state inventory (section 1), the crash section (the diagnostic relink, the gdb stack: `NifItem::deleteChildItems()` under `BaseModel::~BaseModel()` inside the model loader with every other worker in the same parser, 5 of 5 runs), and `scratchpad/bakeperf1_20260911/` (the bisect scripts, the diagnostic-link recipe, the crash logs); `.claude/skills/nifskope-ww-crash-diagnose/SKILL.md` and `.claude/skills/ww-parallelise-a-stage/SKILL.md` (BAKEPERF1 wrote both from this exact problem); the model layer: `src/model/nifitem.{h,cpp}` (the class-level slab pool: 8192-slot chunks, intrusive free list, ONE mutex -- read the 2026-07-20 note in the memory below), `src/model/basemodel.{h,cpp}`, `src/model/nifmodel.{h,cpp}` (`loadIndex`, `load`, the condition cache, `getConditionCacheItem`, `evalCondition`, `invalidateDependentConditions`, static QHash caches, the version/xml lookups), `src/xml/*` (the nif.xml compound/block templates: are they read-only after startup, and is their first use lazy?), `src/data/niftypes.*` (static tables), `src/lodgen.cpp` `lodgenLoadModel` + its cache and `lodgenNativeLoadModel` (READ ONLY -- CARDS-AGG owns that file; any change there goes through a `--check` hook-up script applied only after its DONE), `src/lodgenchunkpass.*` and `src/lodgenparallel.*` (the pool, the retire order, `--chunk-threads`); the memory note at `C:\Users\bungo\.claude\projects\E--Projects-Claude\memory\project_claudenifskope.md` (read-only) for the NifItem slab pool ("mutex -- xmlcheck parses on worker threads") and the model-layer landmines (holdUpdates, dataChanged storms, the row-0 shared condition cache); `MISTAKES.md` root 2026-09-11.
- Skills you MUST invoke: `nifskope-ww-crash-diagnose` (the diagnostic relink and the gdb batch ARE your instrument -- name the fault before fixing), `ww-parallelise-a-stage` (the inventory must follow the CALL, not the file; the byte-identity gate with its refuter), `ww-anchored-hookup`, `nifskope-ww-build-verify` (header changes in the model layer => every TU including them gets its object checked; the exe-newer sweep over the whole set), `nifskope-ww-lodgen`, `ww-test-harness-add` (a new harness needs a floor that fires), `fo4cs-census-field` (the `threads`/`chunkThreads` census words already exist -- they must MOVE).

## bungo's words
- 2026-09-11 15:5x, to "the one thing that would unlock the machine is making the model loader thread-safe ... gated by the same byte-identity chain plus twenty consecutive 16-thread runs without a fault": **"Yeah, go for it"**.
- 10:1x: "bake time, anything we can do to speed it up? use my system to its fullest here?" -- 16 logical cores, 31 GB.

## The work
1. **Name the fault, with the instrument, before any fix** (report section 1): the diagnostic relink (symbols on), gdb batch under `--chunk-threads 16` on the 25-chunk region BAKEPERF1 used, the stack of the first fault AND of two more runs (the stacks must agree or the report says they do not); then the shared state that stack touches, by CALL path: the slab pool's mutex (is `operator delete` locked for every path? is `deleteChildItems` re-entering the pool while another thread allocates?), the condition cache (`getConditionCacheItem` shares row 0 across rows -- is that per model or static?), any static/global in `nifmodel.cpp`/`basemodel.cpp`/`nifitem.cpp`/the xml layer (`static QHash`, function-local statics initialised lazily, `QStringLiteral` is fine, `QString` interning is fine), Qt signals across threads (a model constructed on a worker thread has thread affinity there -- who connects to it? `lodgenLoadModel` creates models with NO views, but `NifModel` may emit into nothing -- confirm), and the resource stack (`BSA`/archive readers: are they locked? `DDSTexture16` decode?). Print the inventory as a table: object, kind (static | per-model | pool), touched by (call path), protected by (mutex | none), verdict.
2. **The fix, smallest first**: prefer (a) making the shared things safe (a lock around the slab pool's every entry, a per-thread or per-model condition cache, the xml templates proven immutable after a one-time init done BEFORE the fan-out) over (b) per-thread copies of whole subsystems over (c) a parse-only critical section (which is what BAKEPERF1 measured as "slow"). If (a) needs a lock on a hot path, measure the cost with and without on the serial bake (stage times) -- a lock that costs the serial bake 5 percent is a refusal with numbers. No behaviour change on the serial path: `--threads 1` output byte-identical to the rung.
3. **The gate**: `--chunk-threads 16` on both regions (9-chunk Sanctuary, 25-chunk Boston): **20 consecutive runs each, no fault, exit 0** (the loop prints each run's exit code and wall time; one NTSTATUS exit = the gate is red and the report says at which run); byte identity of every output file vs the serial run on both regions (BAKEPERF1's comparator, with its refuter shown red first); the stage-time table serial vs 16 threads, medians of 3, with the speed-up per stage -- THE NUMBER bungo asked for; peak working set at 16 threads on 25 chunks (BAKEPERF1 measured 21.9 GB with the parse serialised; if yours is higher, the chunk-thread default must be chosen so the Commonwealth fits in 31 GB with room -- state the rule); ONE ~100-chunk run (the memory curve BAKEPERF1 refused to extrapolate from two points -- give it the third point; still not the whole Commonwealth).
4. **Default**: if the gate is green and the speed-up is real, `--chunk-threads` defaults to the machine's budget (or a memory-derived cap; state it) and the panel's summary line prints it; `--chunk-threads 1` stays the exact way back (byte-identical). If the gate is not green, the default stays 1 and the report says why with the numbers -- a valid deliverable.
5. Build (after CARDS-AGG's DONE), then the chain: the 20x loops, identity, stage times, `lodgen_native.sh`, `lodgen_terrain.sh`, `lodgen_terrain_vt.sh`, `lodgen_roads.sh`, `lodgen_panel_run.sh`, `lod_generation.sh`, `lodgen_stage_times.sh`, the card/array suites, `ui_align.sh`, `water_ui.sh`, AND the GUI-side model harnesses the model layer reaches (`loaded_nifs`, `WW_DUPFREEZE_TEST`, `WW_JOIN_TEST`, `WW_SEP_TEST`, `WW_COPYPASTE_TEST`, `WW_VERTEXFLAGS_TEST` -- find their spells; the model layer serves the whole editor, and a lock added to the pool must not freeze an interactive edit: measure the 38k-vert torso load time before/after, `tools/join_test` etc. at their baselines). Every moved count explained by name.
6. Documents: `scratchpad/nifparse1_20260911/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md` (entries start with `## `); report `scratchpad/lane_nifparse1_report.md`; a dated paragraph appended to `scratchpad/lodui1_20260911/BAKE_INSTRUCTION.md` with the new expected bake time.

## Gates
- N1 the fault named from a symbolised stack (3 runs agree) BEFORE the fix; the inventory table by call path.
- N2 20 consecutive 16-thread runs on each region, no fault; identity vs serial on every file; refuter shown red.
- N3 stage-time table with speed-ups; peak working set; the 100-chunk point.
- N4 serial path byte-identical to the rung; editor harnesses at baseline; torso load time within 5 percent.
- N5 exe newer than every changed file; every TU including a changed model header rebuilt; drivers rebuilt; rung == CARDS-AGG's exe bytes.
- N6 no NifSkope left running; game down at every launch; no crash dialog ever (the error mode stays set).

## Rules
- Code-only until CARDS-AGG's DONE. You own `src/model/*`, `src/xml/*`, `src/data/*` as they relate to loading, `src/lodgenchunkpass.*`, `src/lodgenparallel.*`; `src/lodgen.cpp` and the rest of CARDS-AGG's set only through a `--check` hook-up script applied after its DONE. One build (+ counted relinks). No arithmetic change anywhere. Never his installed files. Never `git stash`, never commit. Plain language.

## Report
`scratchpad/lane_nifparse1_report.md`, incremental (PENDING.md first past half context): `## 0. Pre-registered gates`, `## 1. The fault, named` (stacks, inventory table), `## 2. The fix` (what was made safe, and what it costs the serial path), `## 3. Build and gates` (the 20x loops, identity, stage table, memory, editor harnesses), `## 4. Owed / red / bungo's calls`, `## 5. Mistakes`, `## 6. Finished-work skill review`.
