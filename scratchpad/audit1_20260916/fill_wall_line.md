Three fixture regions through the FO4CS target with defaults: **45 / 50 / 73 s** at **2.00 / 2.13 / 2.68 GB** peak working set, and **meshes is the long pole every time** (39.0 / 39.5 / 59.7 s). The stock target on the same three regions is **byte-identical to the rung exe**, every file but the bake record, whose format changed under BAKEREC1. A null `--incremental` over my own fresh bake replays all 9 chunks, writes 2 files, and is **55 of 55 byte-identical** in 30 s against 45 s -- and still rebuilds the object library, in words.
