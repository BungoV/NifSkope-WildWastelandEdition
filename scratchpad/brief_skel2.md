# Lane SKEL2 -- the skeleton overlay mirrors the Skeleton Manager, both views improved, Blender octahedral bones in blue

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`, working tree. Nothing is committed (CONSTITUTION 8). Exe at launch: `release/NifSkope.exe` 2026-09-11 07:06:04, 20,867,584 B (UI6), carrying every landed change. Rollback rung to take ONCE before your first link: `release/NifSkope.before_skel2.exe` (copy of the 07:06:04 bytes; never overwrite it).
- Read first: `CONSTITUTION.md`; `HANDOFF.md` top block -- the RESUME paragraph, then the "RULING bungo 2026-09-11 05:5x -> 06:0x, LANE SKEL2" paragraph (this lane's law, with his verbatim words, the Blender reference picture and the BLUE ruling), the BUILD11/SKELFIX paragraphs (the overlay's armature rule, the marker-only nodes, the flaky picture gates (c)/(e), the 53 stray pixels), the UI6 block (the newest build state); `scratchpad/lane_skelfix_report.md` in full (the overlay as built: `GLView::refreshSkeletonOverlay` / `drawSkeletonOverlay` / `skeletonOverlayRule` in `src/glview.cpp` ~2193-2500; the 19 marker-only nodes; the gates in `src/skeloverlaytest.cpp` + `tests/spells/skeleton_overlay.sh`); `scratchpad/lane_build11_report.md` sections on the overlay reds; `src/skeletontools.{h,cpp}` (the Skeleton Manager dock: chips All / Bones / Deforming / Unused, search, the Bone / Shapes / Verts / Weight columns, `WW_SKELETON_TEST`); `src/posetools.cpp` + `GLView::drawPoseSkeleton` (~2060) and `refreshPoseBones` (~1476) -- the SECOND skeleton renderer (Pose Mode: capped bones, joint dots, depth fade, hover name, weight overlay, pins, relations); `MISTAKES.md` from the 2026-09-10 entries to the end.
- Skills you MUST invoke (repo tree `.claude/skills/`, identical to the live tree at launch): `nifskope-ww-panel-style` (the manager's list is a panel: shared helpers, no blurbs, colour from the skin palette, self-test counts with floors), `ww-test-harness-add` (every new check with a floor that fires live; the translation-unit rule; the fixture-path traps), `nifskope-ww-render-shot` (headless renders through the render hook for the bone pictures; one instance; second monitor; invisible), `nifskope-ww-build-verify` (the chain; rename-aside immediately before the link; header-staleness sweep -- `glview.h` WILL change, so every TU including it gets its object checked; exe-newer sweep over the whole `git status` set), `ww-anchored-hookup` (edits to `src/nifskope_ui.cpp` and `NifSkope.pro` through a refusing script), `ww-retire-a-surface` if any old drawing path is removed (read it for the seven reachability places), `ww-silhouette-compare` if you find it useful for the bone-shape picture gate.
- Build rules: ONE build (relinks for gate-only fixes are allowed and counted, as UI6 did). `scratchpad/skel2_20260910/BUILDING` up before the build, `DONE` after the gates. `tasklist | grep -i -E "Fallout4|NifSkope"; echo rc=$?` immediately before the link; Fallout4 up = BUILD PENDING with `PENDING.md`. bungo's own window (no `--port`) renamed aside, never killed. No other lane is alive at launch (`ls scratchpad/*/BUILDING` must be empty; say so in the report).

## bungo's words, verbatim (2026-09-11)
- Over a screenshot of `human_male_vanilla.nif` with Overlays > skeleton on (all 130 nodes labelled) beside the Skeleton Manager: **"for that bone view toggle, shouldn't it mirror the skeleton manager view?"**
- **"shouldn't we improve both views (that will now be shared)?"** then, to the director's list (below): **"All good"**.
- **"what about the bone shape? Shouldn't it be something like in Blender?"** and a screenshot of Blender 4.5.3's default armature bone in Object Mode: a solid-shaded octahedron, its square ring about a tenth of the length from the head, tapering to a point at the tail, a ball at the head and a smaller ball at the tail, flat grey with lit facets.
- **"Just keep the color of the bones blue"**.

## The work
1. **One skeleton renderer.** Pose Mode's `drawPoseSkeleton` and the overlay's `drawSkeletonOverlay` become ONE drawing routine (CONSTITUTION 10, shared code), fed by a bone list + per-bone state (selected / active / hovered / pinned / marker-only) that each caller supplies. Pose Mode keeps everything it has (hover name, depth fade, pins, weight overlay, relations, mirror) -- its harnesses `WW_POSEDRAW_TEST` and `WW_POSEEXTRAS_TEST` and the spells that drive them must still pass at their baselines (measure them on the 07:06:04 exe FIRST and record the numbers; `WW_POSEDRAW_TEST` was red before SKELFIX -- if it is red on the rung, say so and do not claim it as yours).
2. **Blender octahedral bones, blue.** Default display = the octahedron from his picture: square ring at 1/10 of the bone length from the head, apex at the tail, head ball, smaller tail ball, solid-shaded with lit facets so the four faces read, drawn with depth so bones inside the mesh are hidden unless X-ray is on. Colour: the skin palette's BLUE the overlay uses today (read it from `wwSkinColor` / the palette, do not invent a value); unselected = that blue at reduced brightness, selected = the blue, active = lighter still, hovered = a rim; non-bone markers stay the muted grey. Display modes: `Octahedral` (default) and `Stick` (the exact way back to today's segment drawing, byte-for-byte in the framebuffer for the same bones); `B-Bone` and `Envelope` may be REFUSED with the reason in the report. An `X-ray` toggle (depth test off for the bones). Where these live in the UI: the Overlays menu (Show Skeleton already lives there) as a submenu, rows only, no text.
3. **Mirror the manager.** The overlay draws EXACTLY the rows the Skeleton Manager lists under its active chip (All / Bones / Deforming / Unused) and its search text. Marker-only (non-bone) nodes appear on All only. Selection is two-way: click a bone in the viewport -> its row is current and visible in the manager; click a row -> the bone is selected in the viewport; double-click a row -> the viewport frames that bone (the existing frame-selected path). Names: only on the hovered and selected bones, or all under a `Names` toggle in the same submenu -- never all 130 by default.
4. **The manager's list improved** (panel-style): the Bone column never elides to nothing -- name column takes the width, the three numeric columns stay fixed on the right, a name is elided only when the dock is narrower than a stated minimum (measure the shipped defect first: on the 07:06:04 exe the rows under `LArm_...` show no name at all in a ~400-px dock); search keeps ancestors visible (the Block List does this already -- reuse the mechanism); rows coloured by kind (deforming bone / helper bone / non-bone node) with the SAME colours the overlay uses (one palette function, called by both); selected and hovered rows match the viewport highlight.
5. **The two grey dots, named.** Frame 46 of the Mixamo clip on the human fixture: two marker-only nodes project at about (486,485) and (463,503) in the 1293x941 run (BUILD11). Print, from the same projection the overlay uses, WHICH nodes those are (names + block numbers); they are two of: AnimObjectA/B/L1-3/R1-3, CamTarget, CamTargetParent, Camera, Camera Control, CharacterBumper, EyeLeftDummy001, PipboyBone, Root, WEAPON, WeaponLeft, skeleton.nif. With item 3, they vanish under the Bones chip; gate (j) is re-registered as "every overlay pixel is on the character under the Bones chip" and the old (j) under All is dropped with the reason.
6. **The flaky gates (c)/(e).** BUILD11 red 3: exact framebuffer equality varied 0..37 px across runs. Give them a measured tolerance (run the same grab 5 times on the rung, take the max spread, set the tolerance above it, and print all five), per `ww-test-harness-add`.
7. Build, then the chain: `skeleton_overlay.sh`, `tests/spells/*pose*` / `poselib.sh` / the spells that drive `WW_POSEDRAW_TEST` and `WW_POSEEXTRAS_TEST`, `WW_SKELETON_TEST`'s spell, `animws.sh` (baseline 72/0, 1 skip), `hkxanim_ui.sh` (48/1), `water_ui.sh` (86/0, floor 75), `ui_align.sh` (11/0), `loaded_nifs` (166/3 -- UI6's known three), `top_bar.sh` (43/5), `files_tab.sh` (28/2). Every count that moves from baseline is explained by check NAME.
8. Pictures through the render hook, same camera, labelled, described in two sentences each before citing: `bones_stick_vs_octa.png` (the same pose, Stick on top, Octahedral below), `bones_bones_chip.png` (Bones chip: no grey dots), `bones_all_chip.png` (All: the dots, named in the caption), `bones_xray.png`, `manager_after.png` (an in-app grab of the dock showing the deep rows WITH names), and `cmp_manager_names.png` (before/after of the elided rows at 2x).
9. Documents: `scratchpad/skel2_20260910/WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md`; report `scratchpad/lane_skel2_report.md`. You do NOT edit `WW_CHANGES.md`, `HANDOFF.md`, `MISTAKES.md`.

## Gates (this lane)
- S1 one renderer: `grep -c` of the old drawing entry points shows `drawPoseSkeleton` and `drawSkeletonOverlay` both delegating to the shared routine (or one removed); Pose Mode harnesses at their recorded baselines.
- S2 Stick mode == the 07:06:04 framebuffer for the same bones and camera within the tolerance of item 6 (the exact way back); Octahedral mode differs from it by > N px (N stated, floor).
- S3 mirror: under each chip, the overlay's drawn node set == the manager's listed set (counts and names, all four chips, and with a search text), with a floor (a deliberately wrong set shown red).
- S4 selection two-way, both directions asserted, plus double-click framing (camera changed).
- S5 manager rows at depth 6+ show a non-empty name at the default dock width; floor = the same rows on the rung show empty.
- S6 the two dots named; Bones chip renders zero pixels off the character mask.
- S7 (c)/(e) tolerance measured from five runs and printed.
- S8 exe newer than every file in `git status --porcelain -- src res tools tests`; header-staleness table for `glview.h` (all TUs); `release/NifSkope.before_skel2.exe` == the 07:06:04 bytes.
- S9 no NifSkope left running by you; bungo's window untouched.

## Rules
- One build (+ gate-only relinks, counted). Files another lane could own go through the refusing hook-up script. Touch nothing in the Animation dock, the LOD panel, the strips.
- Blender is the reference; every divergence from Blender's armature display is named in the report with the reason.
- Never `git stash`, never commit. Plain language in the report.

## Report
`scratchpad/lane_skel2_report.md`, incremental:
- `## 0. Pre-registered gates` (numbers predicted for S1-S7 before code; the baselines measured on the rung)
- `## 1. The two renderers today` (what each draws, what is shared, what Pose Mode needs kept)
- `## 2. The shared renderer and the octahedron` (geometry, shading, colours read from the palette, divergences from Blender)
- `## 3. The mirror and the manager` (chip/search -> node set; the column fix; the palette function)
- `## 4. Build and gates` (mtime table incl. the rung; gate table with baselines and log paths; moved counts by name; skipped harnesses with reasons)
- `## 5. Pictures`
- `## 6. Owed / red / bungo's calls`
- `## 7. Mistakes`
- `## 8. Finished-work skill review` (loaded, missing, written/amended -- name amended skill files for mirroring)
