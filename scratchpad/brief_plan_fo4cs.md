# Lane PLAN-FO4CS -- the FO4CS "Improved LOD" runtime plan, written against the files this generator now writes

## Header
- Tree: `E:\Projects\NifskopeWildWastelandEdition`, branch `main`. Documents only: NO build, NO exe launch, NO change to any file another lane could own. Your files: `docs/FO4CS_IMPROVED_LOD_PLAN.md` (new) and your scratchpad `scratchpad/plan_fo4cs_20260911/`. Lane BAKEPERF1 owns `src/` and `NifSkope.pro` right now; CARDS-AGG follows it. You touch neither.
- FO4CS repo `E:\Projects\Fo4CommunityShaders` is READ-ONLY for you: read its `CONSTITUTION.md`, `Codex\HANDOFF_SESSION_*.md` (newest), `Codex\ROADMAP.md`, the module pattern of one existing module (how a module has a master switch, INI keys, an END-menu row, a census line, a fallback arm -- pick the one whose files best resemble a loader + a draw path), and `Codex\lod-fo4-vs-fo76-comparison.md`. Write nothing there.
- Read first, in order: NifSkope `CONSTITUTION.md`; `HANDOFF.md` top block -- the RESUME paragraph and EVERY "RULING bungo 2026-09-11" paragraph (performance items, gap review, far shadows, identity, emissive, hybrid terrain by band, the FO4CS-side rulings on the 5x5 grid, the screen-size fade spec, zoom, the grid-edge seam list, cross-fade, vanilla LOD suppressed), then the lane blocks NATIVE1a, NATIVE1b, TERRAIN-R, ROADS1, LODUI1, CENSUS1 (what the files ARE today); the contracts IN FULL: `docs/LODGEN_NATIVE_LODO_LODI.md` (v3), `docs/LODGEN_TERRAIN_VT.md`, `docs/LODGEN_BTD_FORMAT.md` (the `.lodl`), `docs/LODGEN_LODM_FORMAT.md`, `docs/LODGEN_CARD_SHEETS.md`, `docs/LODGEN_TEXTURE_ARRAYS.md`, `docs/LODGEN_MANIFEST_FORMAT.md`, `docs/LODGEN_CENSUS.md`; the memory of the campaign at `C:\Users\bungo\.claude\projects\E--Projects-Claude\memory\project_hybrid_lod.md` (read-only) and `project_fo4_community_shaders.md` (read-only, for how FO4CS ships: waves, DLL deploy, INI keys, END menu basics only, RenderDoc capture rounds, the census).
- Skills you MUST invoke: `ww-contract-provenance` (every claim about a file traced to its contract section; hash the contracts first, re-derive last), `ww-census-contract` (the census page is the plan's measurement spine), `fo4cs-census-field`, `fo4cs-menu-row` and `fo4cs-ini-edit` (READ them to know what an FO4CS menu row / INI key may and may not be -- END menu = end-user controls only, policies INI-only; no rows without bungo), `fo4cs-wave-integrate` and `fo4cs-flight-brief` (read, to shape phases as waves with flights), `fo4cs-altered-capture` (read: how a visual claim is proven in FO4CS -- an altered RenderDoc capture, never a test flight).

## bungo's words (verbatim, HANDOFF.md) that the plan must honour
- 2026-09-09 15:34: "This will be a new module for Fo4cs, called Improved LOD" -- ONE module, its own master switch, ships OFF.
- "we're building fo4cs last" -- the plan is the LAST thing built; it consumes, never redefines, the files.
- "This will be solved through FO4CS, vanilla LOD will be suppressed".
- "we'll have far shadows cast by those lods ... Our shadow casters are based on the color id right now I think, so they can only occlude other objects and terrain, never themselves".
- "Dithered cross fade is good"; "Yeah" to emissive at night; "Your solution sounds good" (ring 0 runtime blend, ring 1+ pyramid); the 5x5 grid rounds "Screen size fade to lower LODs would be nice to have within that 5x5 grid, 2 sounds good too, 3 sounds ogod"; "we wanted screen size based lod, right?" = yes; the Unity-style fade thresholds; "note these things" (the four grid-edge seam items).
- CONSTITUTION 10 (both repos): MODULES AND FALLBACKS -- every arm has a floor beneath it, the output names the serving arm, a refusal states its reason.

## The work -- ONE document, `docs/FO4CS_IMPROVED_LOD_PLAN.md`, in this order
1. **Scope and the one rule**: what the module reads (the six file kinds + card/array sheets + manifests, by contract section), what it draws, what it suppresses, and the sentence that it is built last against frozen contracts (name the contract versions and hashes it is planned against).
2. **The rungs, each independently shippable as an FO4CS wave, in the order the far field becomes visible and measurable** -- for each: what it reads (traced), what it draws or does, its master switch and INI keys (policy = INI-only; the ONE END-menu row the module gets is its master switch unless bungo adds more), its fallback arm and the serving-arm word, its census fields (by name from `docs/LODGEN_CENSUS.md`), its gates (pre-registered numbers where the contracts give them), its flight (what bungo does in game and what he sends back), and what it must NOT do (e.g. never alter cell streaming). Rungs, proposed:
   R0 loaders + staleness: read `.lodl` v3, `.lodt`, `.lodo`/`.lodi` v3, `.lodm`; refuse by name; corpus-hash check; the module's census prints the serving arm and STALE names; NOTHING drawn yet. Flight = the census line.
   R1 vanilla LOD suppressed + native far field drawn at ring granularity: instanced draws from `.lodi` sorted order, one draw per (mesh, material) per chunk, identity into the G-buffer the far-shadow pass keys on, the engine's `.bto`/`.btr` draws suppressed for the chunks the module serves (and only those -- the fallback is the engine's own LOD per chunk). Gate: draw counts vs `.lodi` counts (census cross-checks), the identity-keyed far shadow still works, RenderDoc altered-capture picture.
   R2 terrain: `.lodt` pyramid sampling from ring 1 out with residency at a fixed budget; ring 0 runtime blend from the `.lodl` weights per the contract's stated formula; cross-fade across ring 0; the engine's far terrain suppressed. Gate: the ring-0 formula's colour error against the bake (TERRAIN-R printed mean 3.3-3.6/255) reproduced at runtime on the same texel; resident tiles/bytes census.
   R3 screen-size selection + GPU culling: per-cluster cut by projected error against the pixel tolerance (contract 4.4 + the ladder), cluster sphere/cone culling, occluder boxes, shadow-view selection with its own looser tolerance, screen-size fade for the fade classes (Unity-style thresholds, hysteresis), dithered cross-fade at every switch. Gate: triangles per ring vs the reference cut's numbers at the same tolerance/distance (NATIVE1b printed them); cull-rate census.
   R4 cards: per-tree octahedral cards (hemi-octahedral mapping per contract), lit from normal+height, sway from the height channel, shadow from height; aggregate ring-3 cards when CARDS-AGG lands (state the dependency); emissive at night. Gate: a card set rendered in-engine vs the bake's own frame at one view within a stated error.
   R5 the 5x5 grid rounds (AFTER R1-R4 are flown): screen-size fade inside the grid, shadow casters at lower detail from the cluster set, the cluster LOD extended inward with the engine's full draw suppressed per object (the REFR form id join). Each its own wave with its own flight; name the RE work each needs (fade compare site, per-object draw suppression) as candidates, not facts.
3. **The seams**: the four grid-edge items bungo said to note (cross-fade at the grid edge, ring-0 terrain from the finest `.lodl` level, shadow blend band, same fade thresholds inside/outside) -- which rung owns each.
4. **The census as the acceptance instrument**: the fields per rung, the cross-checks the generator half already allows, the first flight's expected numbers where known.
5. **What the generator still owes the runtime** (from CENSUS1's owed list and the lane blocks: per-slot totals, card count, watertight bit, aggregate cards, the decoder's cell rule, the `.BTO` files still written under FO4CS, the resource stack's `.pbrm`/`.lodm` blind spot, the DXT1-vs-DXT5 sheets, splat grading) -- as a table with the lane that owns each, so the FO4CS session never discovers one mid-wave.
6. **Open rulings for bungo** collected in one list (cover's home, the sort inside the cell, REFR id in the cold record, near-model library, `.BTO` under FO4CS, CLI default for candidates, and any the plan itself needs -- e.g. the pixel-tolerance default, the fade thresholds' defaults, the residency budget).
7. **Provenance footer** (`ww-contract-provenance`): contract file hashes and line counts at the time of writing.

## Gates
- G1 every file/field claim cites a contract section that exists (script-checked, as CENSUS1's page gate did; reuse `scratchpad/census1_20260911/page_gate.py`'s approach).
- G2 every rung has all nine parts (reads / does / switch+INI / fallback+arm word / census / gates / flight / must-not / RE candidates where any).
- G3 no invented vocabulary: field names come from `docs/LODGEN_CENSUS.md`, file/table names from the contracts; a glossary section maps any term the FO4CS side uses differently.
- G4 the document is plain language a reader of the FO4CS session can act on without this session's chat.

## Rules
- Documents only. Nothing written outside `docs/FO4CS_IMPROVED_LOD_PLAN.md` and your scratchpad. The FO4CS repo is read-only. Never commit, never git stash.
- Report `scratchpad/lane_plan_fo4cs_report.md`: `## 0. What was read (with hashes)`, `## 1. Decisions the plan makes and why`, `## 2. Gates`, `## 3. Owed`, `## 4. Mistakes`, `## 5. Finished-work skill review`. `WW_CHANGES_ENTRY.md`, `HANDOFF_BLOCK.md`, `MISTAKES_ENTRIES.md` (entries start with `## `) in your scratchpad for the director.
